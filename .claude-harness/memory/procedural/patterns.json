{
  "version": 3,
  "patterns": {
    "codePatterns": [
      "ChildGuard RAII for process cleanup in integration tests",
      "CoreEngine trait in core, transport crates are thin adapters",
      "Deterministic sort by URI then range for stable output",
      "Drop read lock before async publish_diagnostics (deadlock prevention)",
      "FULL text document sync (not INCREMENTAL) for v1 LSP",
      "Hook scripts receive JSON on stdin, output JSON to stdout, use exit 0 for no-op on non-matching files",
      "LSP Content-Length framing vs MCP line-delimited JSON — NEVER mix them",
      "Plugin hooks use hooks.json with {description, hooks: {EventName: [{matcher, hooks: [{type, command, timeout}]}]}} structure",
      "Range containment for nested XML document symbols",
      "Run local SAST baseline (cargo audit, cargo deny, semgrep) before enabling new CI workflow to establish signal baseline",
      "Single-pass stack tokenizer for XML extraction (not multi-regex)",
      "Test files follow pass/fail counter pattern with colored output and exit 1 on any failure",
      "Use slugify(heading.text) for duplicate detection, not heading.slug",
      "When cargo-deny config fails, validate against installed tool version schema before assuming policy error",
      "convert/state/server module separation in markymark-lsp for testability",
      "rmcp CallToolResult::structured and structured_error for MCP payloads"
    ],
    "namingConventions": {
      "hookScripts": "kebab-case.sh (e.g., suggest-lsp.sh)",
      "testFiles": "test_<subject>.sh (e.g., test_hooks.sh) for bash; <handler>_tests.rs for Rust integration",
      "hookConfig": "hooks.json in hooks/ directory",
      "crates": "markymark-<role> (e.g., markymark-core, markymark-parser)"
    },
    "projectSpecificRules": [
      "${CLAUDE_PLUGIN_ROOT} is the standard variable for plugin-relative paths in commands",
      "Checkpoint updates must include claude-progress, working-context, and memory layer persistence",
      "Existing tests use set -euo pipefail - be aware this causes early abort on command failures in non-conditional contexts",
      "Plugin directory is markymark-plugin/ with .claude-plugin/plugin.json manifest",
      "Rust integration tests in tests/ are standalone crates — each file gets its own imports, no mod.rs needed",
      "Shared test helpers must be duplicated in tests/ files (pub(crate) not accessible from integration tests)",
      "Use exact count assertions (assert_eq!) not >= in rename/edit tests — >= masked a closing-tag rename bug",
      "require_marksman! macro for graceful test skip when marksman unavailable in CI"
    ],
    "rustPatterns": [
      {
        "id": "pat-core-contract-first",
        "pattern": "Core-contract-first transport layering",
        "context": "Adding or modifying LSP/MCP transport behavior",
        "application": "Define shared operation trait in core crate first, then make transport crate a forwarding adapter"
      },
      {
        "id": "pat-tree-sitter-cursor",
        "pattern": "Tree-sitter cursor traversal for nested markdown lists",
        "application": "Walk tree-sitter nodes with cursor, collect list_item nodes and recurse into tight_list/loose_list descendants"
      },
      {
        "id": "pat-rmcp-structured-errors",
        "pattern": "rmcp structured error envelope for tool handlers",
        "application": "Validate input first, map core failures by error kind, always return CallToolResult::structured_error"
      },
      {
        "id": "pat-binary-smoke-testing",
        "pattern": "Binary smoke test with RAII cleanup and protocol-aware framing",
        "application": "ChildGuard kill on Drop, thread+channel for timeout (no tokio), Content-Length for LSP, line-delimited for MCP"
      },
      {
        "id": "pat-dual-process-alignment",
        "pattern": "Dual-process LSP alignment testing with response normalization",
        "application": "Normalize URIs to filenames, sort arrays by uri+range, classify as Match/Superset/Mismatch/ServerOnly"
      },
      {
        "id": "pat-lock-safety",
        "pattern": "Drop read lock before async publish_diagnostics",
        "application": "Scope RwLock, collect results into local Vec, drop lock, then call async client method"
      },
      {
        "id": "pat-startup-index-mcp",
        "pattern": "Startup-index runtime adapter for MCP transport",
        "application": "Validate roots once, index at startup, skip unreadable docs, keep runtime execute() read-only"
      },
      {
        "id": "pat-doc-blockers",
        "pattern": "Documentation blockers for unfamiliar crates",
        "application": "Create blocking beads issue, document APIs/patterns/pitfalls first, prefer retrieval-led reasoning"
      },
      {
        "id": "pat-arena-hybrid",
        "pattern": "Hybrid arena ownership for LSP/MCP dual-transport",
        "application": "Per-document Bump for parsed content (re-parse = swap arena). Realm-level owned storage for cross-doc index lookups. hashbrown with bumpalo allocator for HashMap fields."
      },
      {
        "id": "pat-readme-audit",
        "pattern": "Audit README claims against actual implementation before release",
        "application": "Use Explore agent to verify features exist in source code. Check CLI --help for real flags. Cross-reference CI workflows. Remove aspirational claims or mark as upcoming."
      },
      {
        "id": "pat-release-workflow",
        "pattern": "Alpha/prerelease tagging workflow with CI pipeline",
        "context": "Tagging a release to trigger GitHub Actions release.yml",
        "application": "1) Quality gates in parallel, 2) Bump workspace + plugin.json versions, 3) Exact match (=) for prerelease cross-crate deps, 4) Branch + PR + merge (hooks block direct main push), 5) Tag main + push tag, 6) Monitor CI, 7) Verify release artifacts"
      },
      {
        "id": "pat-version-sync",
        "pattern": "Version sync across Cargo workspace and plugin manifest",
        "context": "Bumping project version for any release",
        "application": "Update root Cargo.toml [workspace.package] version, all subcrate path dep versions (use = prefix for prerelease), plugin.json version, and verify with cargo check + cargo run --version"
      }
    ]
  }
}
