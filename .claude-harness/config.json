{
  "version": 3,
  "projectName": "markymark",
  "techStack": "Rust",
  "verification": {
    "build": "cargo build --release",
    "test": "cargo test --workspace",
    "lint": "cargo clippy --workspace --all-targets -- -D warnings",
    "acceptance": ""
  },
  "_note": "Test/lint verification temporarily disabled during arena allocation migration (RED/GREEN TDD cycle). Re-enable after migration completes. Tracked: marky-luy (arena conformance closeout)."
}
