[
  {
    "id": 2812357095,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 98,
    "body": "## Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\n\nDetected 'unsafe' usage, please audit for secure usage\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/69)",
    "created_at": "2026-02-16T13:08:03Z",
    "updated_at": "2026-02-16T13:08:04Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };"
  },
  {
    "id": 2812357100,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 137,
    "body": "## Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\n\nDetected 'unsafe' usage, please audit for secure usage\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/70)",
    "created_at": "2026-02-16T13:08:03Z",
    "updated_at": "2026-02-16T13:08:04Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };\n+        if handle.is_null() {\n+            return Err(KernelError::InternalError(-3));\n+        }\n+\n+        Ok(Self {\n+            handle,\n+            dims,\n+            _not_send_sync: PhantomData,\n+        })\n+    }\n+\n+    /// Add an embedding to the index with the given ID.\n+    ///\n+    /// Both the ID and vector are copied into the Zig index. If an entry with\n+    /// the same ID already exists, its vector is replaced in-place.\n+    ///\n+    /// Returns `Err(InvalidInput)` if:\n+    /// - `id` is empty\n+    /// - `embedding` length does not match the index dimensionality\n+    pub fn add(&mut self, id: &str, embedding: &[f32]) -> Result<(), KernelError> {\n+        if id.is_empty() {\n+            return Err(KernelError::InvalidInput);\n+        }\n+        if embedding.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: handle is valid (created in new(), not yet destroyed).\n+        // id and embedding are valid slices with correct lengths.\n+        let rc = unsafe {\n+            zig_embedding_index_add(\n+                self.handle,\n+                id.as_ptr(),\n+                id.len() as u32,\n+                embedding.as_ptr(),\n+                self.dims,\n+            )\n+        };"
  },
  {
    "id": 2812357108,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 181,
    "body": "## Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\n\nDetected 'unsafe' usage, please audit for secure usage\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/71)",
    "created_at": "2026-02-16T13:08:03Z",
    "updated_at": "2026-02-16T13:08:04Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };\n+        if handle.is_null() {\n+            return Err(KernelError::InternalError(-3));\n+        }\n+\n+        Ok(Self {\n+            handle,\n+            dims,\n+            _not_send_sync: PhantomData,\n+        })\n+    }\n+\n+    /// Add an embedding to the index with the given ID.\n+    ///\n+    /// Both the ID and vector are copied into the Zig index. If an entry with\n+    /// the same ID already exists, its vector is replaced in-place.\n+    ///\n+    /// Returns `Err(InvalidInput)` if:\n+    /// - `id` is empty\n+    /// - `embedding` length does not match the index dimensionality\n+    pub fn add(&mut self, id: &str, embedding: &[f32]) -> Result<(), KernelError> {\n+        if id.is_empty() {\n+            return Err(KernelError::InvalidInput);\n+        }\n+        if embedding.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: handle is valid (created in new(), not yet destroyed).\n+        // id and embedding are valid slices with correct lengths.\n+        let rc = unsafe {\n+            zig_embedding_index_add(\n+                self.handle,\n+                id.as_ptr(),\n+                id.len() as u32,\n+                embedding.as_ptr(),\n+                self.dims,\n+            )\n+        };\n+\n+        match rc {\n+            0 => Ok(()),\n+            -1 => Err(KernelError::InvalidInput),\n+            -3 => Err(KernelError::InternalError(-3)),\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Search the index for the top-K most similar embeddings to `query`.\n+    ///\n+    /// Results are sorted by descending cosine similarity score.\n+    ///\n+    /// Returns an empty vector if `k` is 0 or the index is empty.\n+    /// Returns `Err(InvalidInput)` if `query` length does not match dimensions.\n+    pub fn search(&self, query: &[f32], k: u32) -> Result<Vec<SearchResult>, KernelError> {\n+        if k == 0 {\n+            return Ok(Vec::new());\n+        }\n+        if query.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        let mut result_ids: Vec<*const u8> = vec![std::ptr::null(); k as usize];\n+        let mut result_id_lens: Vec<u32> = vec![0; k as usize];\n+        let mut result_scores: Vec<f32> = vec![0.0; k as usize];\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: handle is valid, query is a valid slice of correct length.\n+        // Output arrays are correctly sized to hold up to k results.\n+        // The returned ID pointers borrow from the index (valid until mutation).\n+        let rc = unsafe {\n+            zig_embedding_index_search(\n+                self.handle,\n+                query.as_ptr(),\n+                self.dims,\n+                result_ids.as_mut_ptr(),\n+                result_id_lens.as_mut_ptr(),\n+                result_scores.as_mut_ptr(),\n+                k,\n+                &mut written,\n+            )\n+        };"
  },
  {
    "id": 2812357114,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 192,
    "body": "## Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\n\nDetected 'unsafe' usage, please audit for secure usage\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/72)",
    "created_at": "2026-02-16T13:08:04Z",
    "updated_at": "2026-02-16T13:08:04Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };\n+        if handle.is_null() {\n+            return Err(KernelError::InternalError(-3));\n+        }\n+\n+        Ok(Self {\n+            handle,\n+            dims,\n+            _not_send_sync: PhantomData,\n+        })\n+    }\n+\n+    /// Add an embedding to the index with the given ID.\n+    ///\n+    /// Both the ID and vector are copied into the Zig index. If an entry with\n+    /// the same ID already exists, its vector is replaced in-place.\n+    ///\n+    /// Returns `Err(InvalidInput)` if:\n+    /// - `id` is empty\n+    /// - `embedding` length does not match the index dimensionality\n+    pub fn add(&mut self, id: &str, embedding: &[f32]) -> Result<(), KernelError> {\n+        if id.is_empty() {\n+            return Err(KernelError::InvalidInput);\n+        }\n+        if embedding.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: handle is valid (created in new(), not yet destroyed).\n+        // id and embedding are valid slices with correct lengths.\n+        let rc = unsafe {\n+            zig_embedding_index_add(\n+                self.handle,\n+                id.as_ptr(),\n+                id.len() as u32,\n+                embedding.as_ptr(),\n+                self.dims,\n+            )\n+        };\n+\n+        match rc {\n+            0 => Ok(()),\n+            -1 => Err(KernelError::InvalidInput),\n+            -3 => Err(KernelError::InternalError(-3)),\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Search the index for the top-K most similar embeddings to `query`.\n+    ///\n+    /// Results are sorted by descending cosine similarity score.\n+    ///\n+    /// Returns an empty vector if `k` is 0 or the index is empty.\n+    /// Returns `Err(InvalidInput)` if `query` length does not match dimensions.\n+    pub fn search(&self, query: &[f32], k: u32) -> Result<Vec<SearchResult>, KernelError> {\n+        if k == 0 {\n+            return Ok(Vec::new());\n+        }\n+        if query.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        let mut result_ids: Vec<*const u8> = vec![std::ptr::null(); k as usize];\n+        let mut result_id_lens: Vec<u32> = vec![0; k as usize];\n+        let mut result_scores: Vec<f32> = vec![0.0; k as usize];\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: handle is valid, query is a valid slice of correct length.\n+        // Output arrays are correctly sized to hold up to k results.\n+        // The returned ID pointers borrow from the index (valid until mutation).\n+        let rc = unsafe {\n+            zig_embedding_index_search(\n+                self.handle,\n+                query.as_ptr(),\n+                self.dims,\n+                result_ids.as_mut_ptr(),\n+                result_id_lens.as_mut_ptr(),\n+                result_scores.as_mut_ptr(),\n+                k,\n+                &mut written,\n+            )\n+        };\n+\n+        match rc {\n+            0 => {\n+                let results = (0..written as usize)\n+                    .map(|i| {\n+                        // SAFETY: The Zig index returned valid pointers into its\n+                        // own storage. We copy them into owned Strings immediately.\n+                        let id_slice = unsafe {\n+                            std::slice::from_raw_parts(result_ids[i], result_id_lens[i] as usize)\n+                        };"
  },
  {
    "id": 2812357121,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 216,
    "body": "## Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\n\nDetected 'unsafe' usage, please audit for secure usage\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/73)",
    "created_at": "2026-02-16T13:08:04Z",
    "updated_at": "2026-02-16T13:08:04Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };\n+        if handle.is_null() {\n+            return Err(KernelError::InternalError(-3));\n+        }\n+\n+        Ok(Self {\n+            handle,\n+            dims,\n+            _not_send_sync: PhantomData,\n+        })\n+    }\n+\n+    /// Add an embedding to the index with the given ID.\n+    ///\n+    /// Both the ID and vector are copied into the Zig index. If an entry with\n+    /// the same ID already exists, its vector is replaced in-place.\n+    ///\n+    /// Returns `Err(InvalidInput)` if:\n+    /// - `id` is empty\n+    /// - `embedding` length does not match the index dimensionality\n+    pub fn add(&mut self, id: &str, embedding: &[f32]) -> Result<(), KernelError> {\n+        if id.is_empty() {\n+            return Err(KernelError::InvalidInput);\n+        }\n+        if embedding.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: handle is valid (created in new(), not yet destroyed).\n+        // id and embedding are valid slices with correct lengths.\n+        let rc = unsafe {\n+            zig_embedding_index_add(\n+                self.handle,\n+                id.as_ptr(),\n+                id.len() as u32,\n+                embedding.as_ptr(),\n+                self.dims,\n+            )\n+        };\n+\n+        match rc {\n+            0 => Ok(()),\n+            -1 => Err(KernelError::InvalidInput),\n+            -3 => Err(KernelError::InternalError(-3)),\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Search the index for the top-K most similar embeddings to `query`.\n+    ///\n+    /// Results are sorted by descending cosine similarity score.\n+    ///\n+    /// Returns an empty vector if `k` is 0 or the index is empty.\n+    /// Returns `Err(InvalidInput)` if `query` length does not match dimensions.\n+    pub fn search(&self, query: &[f32], k: u32) -> Result<Vec<SearchResult>, KernelError> {\n+        if k == 0 {\n+            return Ok(Vec::new());\n+        }\n+        if query.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        let mut result_ids: Vec<*const u8> = vec![std::ptr::null(); k as usize];\n+        let mut result_id_lens: Vec<u32> = vec![0; k as usize];\n+        let mut result_scores: Vec<f32> = vec![0.0; k as usize];\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: handle is valid, query is a valid slice of correct length.\n+        // Output arrays are correctly sized to hold up to k results.\n+        // The returned ID pointers borrow from the index (valid until mutation).\n+        let rc = unsafe {\n+            zig_embedding_index_search(\n+                self.handle,\n+                query.as_ptr(),\n+                self.dims,\n+                result_ids.as_mut_ptr(),\n+                result_id_lens.as_mut_ptr(),\n+                result_scores.as_mut_ptr(),\n+                k,\n+                &mut written,\n+            )\n+        };\n+\n+        match rc {\n+            0 => {\n+                let results = (0..written as usize)\n+                    .map(|i| {\n+                        // SAFETY: The Zig index returned valid pointers into its\n+                        // own storage. We copy them into owned Strings immediately.\n+                        let id_slice = unsafe {\n+                            std::slice::from_raw_parts(result_ids[i], result_id_lens[i] as usize)\n+                        };\n+                        let id = String::from_utf8_lossy(id_slice).into_owned();\n+                        SearchResult {\n+                            id,\n+                            score: result_scores[i],\n+                        }\n+                    })\n+                    .collect();\n+                Ok(results)\n+            }\n+            -1 => Err(KernelError::InvalidInput),\n+            -2 => {\n+                // k==0 case — we handle this above but the Zig layer also\n+                // returns -2 for k==0. Treat as empty result.\n+                Ok(Vec::new())\n+            }\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Return the number of entries in the index.\n+    pub fn count(&self) -> u32 {\n+        // SAFETY: handle is valid.\n+        let rc = unsafe { zig_embedding_index_count(self.handle) };"
  },
  {
    "id": 2812357124,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 236,
    "body": "## Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\n\nDetected 'unsafe' usage, please audit for secure usage\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/74)",
    "created_at": "2026-02-16T13:08:04Z",
    "updated_at": "2026-02-16T13:08:04Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };\n+        if handle.is_null() {\n+            return Err(KernelError::InternalError(-3));\n+        }\n+\n+        Ok(Self {\n+            handle,\n+            dims,\n+            _not_send_sync: PhantomData,\n+        })\n+    }\n+\n+    /// Add an embedding to the index with the given ID.\n+    ///\n+    /// Both the ID and vector are copied into the Zig index. If an entry with\n+    /// the same ID already exists, its vector is replaced in-place.\n+    ///\n+    /// Returns `Err(InvalidInput)` if:\n+    /// - `id` is empty\n+    /// - `embedding` length does not match the index dimensionality\n+    pub fn add(&mut self, id: &str, embedding: &[f32]) -> Result<(), KernelError> {\n+        if id.is_empty() {\n+            return Err(KernelError::InvalidInput);\n+        }\n+        if embedding.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: handle is valid (created in new(), not yet destroyed).\n+        // id and embedding are valid slices with correct lengths.\n+        let rc = unsafe {\n+            zig_embedding_index_add(\n+                self.handle,\n+                id.as_ptr(),\n+                id.len() as u32,\n+                embedding.as_ptr(),\n+                self.dims,\n+            )\n+        };\n+\n+        match rc {\n+            0 => Ok(()),\n+            -1 => Err(KernelError::InvalidInput),\n+            -3 => Err(KernelError::InternalError(-3)),\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Search the index for the top-K most similar embeddings to `query`.\n+    ///\n+    /// Results are sorted by descending cosine similarity score.\n+    ///\n+    /// Returns an empty vector if `k` is 0 or the index is empty.\n+    /// Returns `Err(InvalidInput)` if `query` length does not match dimensions.\n+    pub fn search(&self, query: &[f32], k: u32) -> Result<Vec<SearchResult>, KernelError> {\n+        if k == 0 {\n+            return Ok(Vec::new());\n+        }\n+        if query.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        let mut result_ids: Vec<*const u8> = vec![std::ptr::null(); k as usize];\n+        let mut result_id_lens: Vec<u32> = vec![0; k as usize];\n+        let mut result_scores: Vec<f32> = vec![0.0; k as usize];\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: handle is valid, query is a valid slice of correct length.\n+        // Output arrays are correctly sized to hold up to k results.\n+        // The returned ID pointers borrow from the index (valid until mutation).\n+        let rc = unsafe {\n+            zig_embedding_index_search(\n+                self.handle,\n+                query.as_ptr(),\n+                self.dims,\n+                result_ids.as_mut_ptr(),\n+                result_id_lens.as_mut_ptr(),\n+                result_scores.as_mut_ptr(),\n+                k,\n+                &mut written,\n+            )\n+        };\n+\n+        match rc {\n+            0 => {\n+                let results = (0..written as usize)\n+                    .map(|i| {\n+                        // SAFETY: The Zig index returned valid pointers into its\n+                        // own storage. We copy them into owned Strings immediately.\n+                        let id_slice = unsafe {\n+                            std::slice::from_raw_parts(result_ids[i], result_id_lens[i] as usize)\n+                        };\n+                        let id = String::from_utf8_lossy(id_slice).into_owned();\n+                        SearchResult {\n+                            id,\n+                            score: result_scores[i],\n+                        }\n+                    })\n+                    .collect();\n+                Ok(results)\n+            }\n+            -1 => Err(KernelError::InvalidInput),\n+            -2 => {\n+                // k==0 case — we handle this above but the Zig layer also\n+                // returns -2 for k==0. Treat as empty result.\n+                Ok(Vec::new())\n+            }\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Return the number of entries in the index.\n+    pub fn count(&self) -> u32 {\n+        // SAFETY: handle is valid.\n+        let rc = unsafe { zig_embedding_index_count(self.handle) };\n+        if rc < 0 {\n+            0\n+        } else {\n+            rc as u32\n+        }\n+    }\n+\n+    /// Return the dimensionality of vectors in this index.\n+    pub fn dimensions(&self) -> u32 {\n+        self.dims\n+    }\n+}\n+\n+impl Drop for EmbeddingIndex {\n+    fn drop(&mut self) {\n+        if !self.handle.is_null() {\n+            // SAFETY: handle was created by zig_embedding_index_create and\n+            // has not been destroyed yet. Setting to null prevents double-free.\n+            unsafe { zig_embedding_index_destroy(self.handle) };"
  },
  {
    "id": 2812357130,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/hash.rs",
    "line": 58,
    "body": "## Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\n\nDetected 'unsafe' usage, please audit for secure usage\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/75)",
    "created_at": "2026-02-16T13:08:04Z",
    "updated_at": "2026-02-16T13:08:04Z",
    "diff_hunk": "@@ -1,62 +1,147 @@\n-//! Content hashing (FNV-1a 64-bit).\n+//! Entity hash extraction (FNV-1a word hashing).\n //!\n-//! Wraps the Zig `marky_content_hash` FFI function for computing\n-//! deterministic content fingerprints.\n+//! Wraps the Zig `zig_extract_entity_hashes` FFI function which tokenizes\n+//! text on whitespace/punctuation and produces an FNV-1a u32 hash per word.\n+\n+use crate::scan::KernelError;\n+\n+/// Maximum number of retry attempts when the output buffer is too small.\n+const MAX_RETRIES: u32 = 3;\n+\n+/// Initial buffer capacity for entity hash results.\n+const INITIAL_CAP: usize = 64;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n \n extern \"C\" {\n-    fn marky_content_hash(text: *const u8, len: u32) -> u64;\n+    fn zig_extract_entity_hashes(\n+        text_ptr: *const u8,\n+        text_len: u32,\n+        output_ids: *mut u32,\n+        capacity: u32,\n+        written: *mut u32,\n+    ) -> i32;\n }\n \n-/// FNV offset basis (hash of empty input).\n-pub const FNV_OFFSET_BASIS: u64 = 0xcbf29ce484222325;\n+// ---------------------------------------------------------------------------\n+// Public API\n+// ---------------------------------------------------------------------------\n \n-/// Compute a deterministic FNV-1a 64-bit hash of the given text.\n+/// Extract entity hashes from text using FNV-1a word hashing.\n+///\n+/// Tokenizes text on whitespace and punctuation boundaries, then hashes each\n+/// word with FNV-1a to produce a truncated u32 hash per token. Useful for\n+/// entity-based similarity comparisons (e.g. Jaccard on hash sets).\n ///\n-/// Returns [`FNV_OFFSET_BASIS`] for empty input (the standard FNV-1a\n-/// offset basis). Useful for content deduplication and change detection.\n-pub fn content_hash(text: &str) -> u64 {\n+/// Returns an empty vector for empty input (not an error).\n+pub fn extract_entity_hashes(text: &str) -> Result<Vec<u32>, KernelError> {\n     if text.is_empty() {\n-        return FNV_OFFSET_BASIS;\n+        return Ok(Vec::new());\n     }\n-    // SAFETY: text.as_ptr() is valid for text.len() bytes.\n-    // marky_content_hash is a pure function with no side effects.\n-    unsafe { marky_content_hash(text.as_ptr(), text.len() as u32) }\n+\n+    let mut buf: Vec<u32> = vec![0; INITIAL_CAP];\n+    let text_bytes = text.as_bytes();\n+    let text_ptr = text_bytes.as_ptr();\n+    let text_len = text_bytes.len() as u32;\n+\n+    for _ in 0..=MAX_RETRIES {\n+        let cap = buf.len() as u32;\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: text_ptr valid for text_len bytes, buf has capacity cap,\n+        // written is a valid mutable reference. FFI writes at most cap u32s.\n+        let rc = unsafe {\n+            zig_extract_entity_hashes(text_ptr, text_len, buf.as_mut_ptr(), cap, &mut written)\n+        };"
  },
  {
    "id": 2812357136,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/similarity.rs",
    "line": 44,
    "body": "## Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\n\nDetected 'unsafe' usage, please audit for secure usage\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/76)",
    "created_at": "2026-02-16T13:08:04Z",
    "updated_at": "2026-02-16T13:08:04Z",
    "diff_hunk": "@@ -1,4 +1,189 @@\n-//! Similarity computations.\n+//! Similarity computations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_cosine_similarity` and\n-//! `zig_jaccard_similarity` FFI functions.\n+//! Wraps the Zig `zig_cosine_similarity` and `zig_jaccard_similarity` FFI\n+//! functions exported from `c_adapter.zig`.\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_cosine_similarity(a: *const f32, b: *const f32, dims: u32) -> f32;\n+    fn zig_jaccard_similarity(\n+        set1: *const u32,\n+        set1_len: u32,\n+        set2: *const u32,\n+        set2_len: u32,\n+    ) -> f32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Public API\n+// ---------------------------------------------------------------------------\n+\n+/// Compute the cosine similarity between two f32 vectors.\n+///\n+/// Both vectors must have the same length (`dims`). Returns a value in\n+/// `[-1.0, 1.0]` where 1.0 means identical direction.\n+///\n+/// Returns `Err(InvalidInput)` if either slice is empty or lengths differ.\n+pub fn cosine_similarity(a: &[f32], b: &[f32]) -> Result<f32, KernelError> {\n+    if a.is_empty() || b.is_empty() {\n+        return Err(KernelError::InvalidInput);\n+    }\n+    if a.len() != b.len() {\n+        return Err(KernelError::InvalidInput);\n+    }\n+\n+    // SAFETY: a and b are valid slices with matching lengths.\n+    // zig_cosine_similarity returns -2.0 on null pointer (cannot happen here)\n+    // or on dims==0 (checked above).\n+    let result = unsafe { zig_cosine_similarity(a.as_ptr(), b.as_ptr(), a.len() as u32) };"
  },
  {
    "id": 2812357142,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/similarity.rs",
    "line": 77,
    "body": "## Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\n\nDetected 'unsafe' usage, please audit for secure usage\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/77)",
    "created_at": "2026-02-16T13:08:04Z",
    "updated_at": "2026-02-16T13:08:04Z",
    "diff_hunk": "@@ -1,4 +1,189 @@\n-//! Similarity computations.\n+//! Similarity computations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_cosine_similarity` and\n-//! `zig_jaccard_similarity` FFI functions.\n+//! Wraps the Zig `zig_cosine_similarity` and `zig_jaccard_similarity` FFI\n+//! functions exported from `c_adapter.zig`.\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_cosine_similarity(a: *const f32, b: *const f32, dims: u32) -> f32;\n+    fn zig_jaccard_similarity(\n+        set1: *const u32,\n+        set1_len: u32,\n+        set2: *const u32,\n+        set2_len: u32,\n+    ) -> f32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Public API\n+// ---------------------------------------------------------------------------\n+\n+/// Compute the cosine similarity between two f32 vectors.\n+///\n+/// Both vectors must have the same length (`dims`). Returns a value in\n+/// `[-1.0, 1.0]` where 1.0 means identical direction.\n+///\n+/// Returns `Err(InvalidInput)` if either slice is empty or lengths differ.\n+pub fn cosine_similarity(a: &[f32], b: &[f32]) -> Result<f32, KernelError> {\n+    if a.is_empty() || b.is_empty() {\n+        return Err(KernelError::InvalidInput);\n+    }\n+    if a.len() != b.len() {\n+        return Err(KernelError::InvalidInput);\n+    }\n+\n+    // SAFETY: a and b are valid slices with matching lengths.\n+    // zig_cosine_similarity returns -2.0 on null pointer (cannot happen here)\n+    // or on dims==0 (checked above).\n+    let result = unsafe { zig_cosine_similarity(a.as_ptr(), b.as_ptr(), a.len() as u32) };\n+\n+    // The Zig function returns -2.0 as a sentinel for invalid input.\n+    if result == -2.0 {\n+        return Err(KernelError::InvalidInput);\n+    }\n+\n+    Ok(result)\n+}\n+\n+/// Compute the Jaccard similarity between two sorted u32 sets.\n+///\n+/// Both sets must be sorted in ascending order. Returns a value in `[0.0, 1.0]`\n+/// where 1.0 means identical sets.\n+///\n+/// Returns `Ok(0.0)` if both sets are empty (vacuously similar).\n+/// Returns `Ok(0.0)` if one set is empty and the other is not.\n+pub fn jaccard_similarity(set1: &[u32], set2: &[u32]) -> Result<f32, KernelError> {\n+    // Both empty → 0.0 (Zig returns 0.0 for empty sets)\n+    if set1.is_empty() && set2.is_empty() {\n+        return Ok(0.0);\n+    }\n+\n+    // SAFETY: set1 and set2 are valid slices. zig_jaccard_similarity returns\n+    // -1.0 on null pointer (cannot happen with valid slice pointers).\n+    let result = unsafe {\n+        zig_jaccard_similarity(\n+            set1.as_ptr(),\n+            set1.len() as u32,\n+            set2.as_ptr(),\n+            set2.len() as u32,\n+        )\n+    };"
  },
  {
    "id": 2812361125,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/similarity.rs",
    "line": 77,
    "body": "## Semgrep Finding: semgrep.markymark.rust.unsafe-block\n\nUnsafe block detected. Require explicit safety justification and review.\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/86)",
    "created_at": "2026-02-16T13:09:05Z",
    "updated_at": "2026-02-16T13:09:06Z",
    "diff_hunk": "@@ -1,4 +1,189 @@\n-//! Similarity computations.\n+//! Similarity computations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_cosine_similarity` and\n-//! `zig_jaccard_similarity` FFI functions.\n+//! Wraps the Zig `zig_cosine_similarity` and `zig_jaccard_similarity` FFI\n+//! functions exported from `c_adapter.zig`.\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_cosine_similarity(a: *const f32, b: *const f32, dims: u32) -> f32;\n+    fn zig_jaccard_similarity(\n+        set1: *const u32,\n+        set1_len: u32,\n+        set2: *const u32,\n+        set2_len: u32,\n+    ) -> f32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Public API\n+// ---------------------------------------------------------------------------\n+\n+/// Compute the cosine similarity between two f32 vectors.\n+///\n+/// Both vectors must have the same length (`dims`). Returns a value in\n+/// `[-1.0, 1.0]` where 1.0 means identical direction.\n+///\n+/// Returns `Err(InvalidInput)` if either slice is empty or lengths differ.\n+pub fn cosine_similarity(a: &[f32], b: &[f32]) -> Result<f32, KernelError> {\n+    if a.is_empty() || b.is_empty() {\n+        return Err(KernelError::InvalidInput);\n+    }\n+    if a.len() != b.len() {\n+        return Err(KernelError::InvalidInput);\n+    }\n+\n+    // SAFETY: a and b are valid slices with matching lengths.\n+    // zig_cosine_similarity returns -2.0 on null pointer (cannot happen here)\n+    // or on dims==0 (checked above).\n+    let result = unsafe { zig_cosine_similarity(a.as_ptr(), b.as_ptr(), a.len() as u32) };\n+\n+    // The Zig function returns -2.0 as a sentinel for invalid input.\n+    if result == -2.0 {\n+        return Err(KernelError::InvalidInput);\n+    }\n+\n+    Ok(result)\n+}\n+\n+/// Compute the Jaccard similarity between two sorted u32 sets.\n+///\n+/// Both sets must be sorted in ascending order. Returns a value in `[0.0, 1.0]`\n+/// where 1.0 means identical sets.\n+///\n+/// Returns `Ok(0.0)` if both sets are empty (vacuously similar).\n+/// Returns `Ok(0.0)` if one set is empty and the other is not.\n+pub fn jaccard_similarity(set1: &[u32], set2: &[u32]) -> Result<f32, KernelError> {\n+    // Both empty → 0.0 (Zig returns 0.0 for empty sets)\n+    if set1.is_empty() && set2.is_empty() {\n+        return Ok(0.0);\n+    }\n+\n+    // SAFETY: set1 and set2 are valid slices. zig_jaccard_similarity returns\n+    // -1.0 on null pointer (cannot happen with valid slice pointers).\n+    let result = unsafe {\n+        zig_jaccard_similarity(\n+            set1.as_ptr(),\n+            set1.len() as u32,\n+            set2.as_ptr(),\n+            set2.len() as u32,\n+        )\n+    };"
  },
  {
    "id": 2812361138,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/similarity.rs",
    "line": 44,
    "body": "## Semgrep Finding: semgrep.markymark.rust.unsafe-block\n\nUnsafe block detected. Require explicit safety justification and review.\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/85)",
    "created_at": "2026-02-16T13:09:05Z",
    "updated_at": "2026-02-16T13:09:06Z",
    "diff_hunk": "@@ -1,4 +1,189 @@\n-//! Similarity computations.\n+//! Similarity computations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_cosine_similarity` and\n-//! `zig_jaccard_similarity` FFI functions.\n+//! Wraps the Zig `zig_cosine_similarity` and `zig_jaccard_similarity` FFI\n+//! functions exported from `c_adapter.zig`.\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_cosine_similarity(a: *const f32, b: *const f32, dims: u32) -> f32;\n+    fn zig_jaccard_similarity(\n+        set1: *const u32,\n+        set1_len: u32,\n+        set2: *const u32,\n+        set2_len: u32,\n+    ) -> f32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Public API\n+// ---------------------------------------------------------------------------\n+\n+/// Compute the cosine similarity between two f32 vectors.\n+///\n+/// Both vectors must have the same length (`dims`). Returns a value in\n+/// `[-1.0, 1.0]` where 1.0 means identical direction.\n+///\n+/// Returns `Err(InvalidInput)` if either slice is empty or lengths differ.\n+pub fn cosine_similarity(a: &[f32], b: &[f32]) -> Result<f32, KernelError> {\n+    if a.is_empty() || b.is_empty() {\n+        return Err(KernelError::InvalidInput);\n+    }\n+    if a.len() != b.len() {\n+        return Err(KernelError::InvalidInput);\n+    }\n+\n+    // SAFETY: a and b are valid slices with matching lengths.\n+    // zig_cosine_similarity returns -2.0 on null pointer (cannot happen here)\n+    // or on dims==0 (checked above).\n+    let result = unsafe { zig_cosine_similarity(a.as_ptr(), b.as_ptr(), a.len() as u32) };"
  },
  {
    "id": 2812361144,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/hash.rs",
    "line": 58,
    "body": "## Semgrep Finding: semgrep.markymark.rust.unsafe-block\n\nUnsafe block detected. Require explicit safety justification and review.\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/84)",
    "created_at": "2026-02-16T13:09:06Z",
    "updated_at": "2026-02-16T13:09:06Z",
    "diff_hunk": "@@ -1,62 +1,147 @@\n-//! Content hashing (FNV-1a 64-bit).\n+//! Entity hash extraction (FNV-1a word hashing).\n //!\n-//! Wraps the Zig `marky_content_hash` FFI function for computing\n-//! deterministic content fingerprints.\n+//! Wraps the Zig `zig_extract_entity_hashes` FFI function which tokenizes\n+//! text on whitespace/punctuation and produces an FNV-1a u32 hash per word.\n+\n+use crate::scan::KernelError;\n+\n+/// Maximum number of retry attempts when the output buffer is too small.\n+const MAX_RETRIES: u32 = 3;\n+\n+/// Initial buffer capacity for entity hash results.\n+const INITIAL_CAP: usize = 64;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n \n extern \"C\" {\n-    fn marky_content_hash(text: *const u8, len: u32) -> u64;\n+    fn zig_extract_entity_hashes(\n+        text_ptr: *const u8,\n+        text_len: u32,\n+        output_ids: *mut u32,\n+        capacity: u32,\n+        written: *mut u32,\n+    ) -> i32;\n }\n \n-/// FNV offset basis (hash of empty input).\n-pub const FNV_OFFSET_BASIS: u64 = 0xcbf29ce484222325;\n+// ---------------------------------------------------------------------------\n+// Public API\n+// ---------------------------------------------------------------------------\n \n-/// Compute a deterministic FNV-1a 64-bit hash of the given text.\n+/// Extract entity hashes from text using FNV-1a word hashing.\n+///\n+/// Tokenizes text on whitespace and punctuation boundaries, then hashes each\n+/// word with FNV-1a to produce a truncated u32 hash per token. Useful for\n+/// entity-based similarity comparisons (e.g. Jaccard on hash sets).\n ///\n-/// Returns [`FNV_OFFSET_BASIS`] for empty input (the standard FNV-1a\n-/// offset basis). Useful for content deduplication and change detection.\n-pub fn content_hash(text: &str) -> u64 {\n+/// Returns an empty vector for empty input (not an error).\n+pub fn extract_entity_hashes(text: &str) -> Result<Vec<u32>, KernelError> {\n     if text.is_empty() {\n-        return FNV_OFFSET_BASIS;\n+        return Ok(Vec::new());\n     }\n-    // SAFETY: text.as_ptr() is valid for text.len() bytes.\n-    // marky_content_hash is a pure function with no side effects.\n-    unsafe { marky_content_hash(text.as_ptr(), text.len() as u32) }\n+\n+    let mut buf: Vec<u32> = vec![0; INITIAL_CAP];\n+    let text_bytes = text.as_bytes();\n+    let text_ptr = text_bytes.as_ptr();\n+    let text_len = text_bytes.len() as u32;\n+\n+    for _ in 0..=MAX_RETRIES {\n+        let cap = buf.len() as u32;\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: text_ptr valid for text_len bytes, buf has capacity cap,\n+        // written is a valid mutable reference. FFI writes at most cap u32s.\n+        let rc = unsafe {\n+            zig_extract_entity_hashes(text_ptr, text_len, buf.as_mut_ptr(), cap, &mut written)\n+        };"
  },
  {
    "id": 2812361154,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 236,
    "body": "## Semgrep Finding: semgrep.markymark.rust.unsafe-block\n\nUnsafe block detected. Require explicit safety justification and review.\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/83)",
    "created_at": "2026-02-16T13:09:06Z",
    "updated_at": "2026-02-16T13:09:06Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };\n+        if handle.is_null() {\n+            return Err(KernelError::InternalError(-3));\n+        }\n+\n+        Ok(Self {\n+            handle,\n+            dims,\n+            _not_send_sync: PhantomData,\n+        })\n+    }\n+\n+    /// Add an embedding to the index with the given ID.\n+    ///\n+    /// Both the ID and vector are copied into the Zig index. If an entry with\n+    /// the same ID already exists, its vector is replaced in-place.\n+    ///\n+    /// Returns `Err(InvalidInput)` if:\n+    /// - `id` is empty\n+    /// - `embedding` length does not match the index dimensionality\n+    pub fn add(&mut self, id: &str, embedding: &[f32]) -> Result<(), KernelError> {\n+        if id.is_empty() {\n+            return Err(KernelError::InvalidInput);\n+        }\n+        if embedding.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: handle is valid (created in new(), not yet destroyed).\n+        // id and embedding are valid slices with correct lengths.\n+        let rc = unsafe {\n+            zig_embedding_index_add(\n+                self.handle,\n+                id.as_ptr(),\n+                id.len() as u32,\n+                embedding.as_ptr(),\n+                self.dims,\n+            )\n+        };\n+\n+        match rc {\n+            0 => Ok(()),\n+            -1 => Err(KernelError::InvalidInput),\n+            -3 => Err(KernelError::InternalError(-3)),\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Search the index for the top-K most similar embeddings to `query`.\n+    ///\n+    /// Results are sorted by descending cosine similarity score.\n+    ///\n+    /// Returns an empty vector if `k` is 0 or the index is empty.\n+    /// Returns `Err(InvalidInput)` if `query` length does not match dimensions.\n+    pub fn search(&self, query: &[f32], k: u32) -> Result<Vec<SearchResult>, KernelError> {\n+        if k == 0 {\n+            return Ok(Vec::new());\n+        }\n+        if query.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        let mut result_ids: Vec<*const u8> = vec![std::ptr::null(); k as usize];\n+        let mut result_id_lens: Vec<u32> = vec![0; k as usize];\n+        let mut result_scores: Vec<f32> = vec![0.0; k as usize];\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: handle is valid, query is a valid slice of correct length.\n+        // Output arrays are correctly sized to hold up to k results.\n+        // The returned ID pointers borrow from the index (valid until mutation).\n+        let rc = unsafe {\n+            zig_embedding_index_search(\n+                self.handle,\n+                query.as_ptr(),\n+                self.dims,\n+                result_ids.as_mut_ptr(),\n+                result_id_lens.as_mut_ptr(),\n+                result_scores.as_mut_ptr(),\n+                k,\n+                &mut written,\n+            )\n+        };\n+\n+        match rc {\n+            0 => {\n+                let results = (0..written as usize)\n+                    .map(|i| {\n+                        // SAFETY: The Zig index returned valid pointers into its\n+                        // own storage. We copy them into owned Strings immediately.\n+                        let id_slice = unsafe {\n+                            std::slice::from_raw_parts(result_ids[i], result_id_lens[i] as usize)\n+                        };\n+                        let id = String::from_utf8_lossy(id_slice).into_owned();\n+                        SearchResult {\n+                            id,\n+                            score: result_scores[i],\n+                        }\n+                    })\n+                    .collect();\n+                Ok(results)\n+            }\n+            -1 => Err(KernelError::InvalidInput),\n+            -2 => {\n+                // k==0 case — we handle this above but the Zig layer also\n+                // returns -2 for k==0. Treat as empty result.\n+                Ok(Vec::new())\n+            }\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Return the number of entries in the index.\n+    pub fn count(&self) -> u32 {\n+        // SAFETY: handle is valid.\n+        let rc = unsafe { zig_embedding_index_count(self.handle) };\n+        if rc < 0 {\n+            0\n+        } else {\n+            rc as u32\n+        }\n+    }\n+\n+    /// Return the dimensionality of vectors in this index.\n+    pub fn dimensions(&self) -> u32 {\n+        self.dims\n+    }\n+}\n+\n+impl Drop for EmbeddingIndex {\n+    fn drop(&mut self) {\n+        if !self.handle.is_null() {\n+            // SAFETY: handle was created by zig_embedding_index_create and\n+            // has not been destroyed yet. Setting to null prevents double-free.\n+            unsafe { zig_embedding_index_destroy(self.handle) };"
  },
  {
    "id": 2812361161,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 216,
    "body": "## Semgrep Finding: semgrep.markymark.rust.unsafe-block\n\nUnsafe block detected. Require explicit safety justification and review.\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/82)",
    "created_at": "2026-02-16T13:09:06Z",
    "updated_at": "2026-02-16T13:09:06Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };\n+        if handle.is_null() {\n+            return Err(KernelError::InternalError(-3));\n+        }\n+\n+        Ok(Self {\n+            handle,\n+            dims,\n+            _not_send_sync: PhantomData,\n+        })\n+    }\n+\n+    /// Add an embedding to the index with the given ID.\n+    ///\n+    /// Both the ID and vector are copied into the Zig index. If an entry with\n+    /// the same ID already exists, its vector is replaced in-place.\n+    ///\n+    /// Returns `Err(InvalidInput)` if:\n+    /// - `id` is empty\n+    /// - `embedding` length does not match the index dimensionality\n+    pub fn add(&mut self, id: &str, embedding: &[f32]) -> Result<(), KernelError> {\n+        if id.is_empty() {\n+            return Err(KernelError::InvalidInput);\n+        }\n+        if embedding.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: handle is valid (created in new(), not yet destroyed).\n+        // id and embedding are valid slices with correct lengths.\n+        let rc = unsafe {\n+            zig_embedding_index_add(\n+                self.handle,\n+                id.as_ptr(),\n+                id.len() as u32,\n+                embedding.as_ptr(),\n+                self.dims,\n+            )\n+        };\n+\n+        match rc {\n+            0 => Ok(()),\n+            -1 => Err(KernelError::InvalidInput),\n+            -3 => Err(KernelError::InternalError(-3)),\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Search the index for the top-K most similar embeddings to `query`.\n+    ///\n+    /// Results are sorted by descending cosine similarity score.\n+    ///\n+    /// Returns an empty vector if `k` is 0 or the index is empty.\n+    /// Returns `Err(InvalidInput)` if `query` length does not match dimensions.\n+    pub fn search(&self, query: &[f32], k: u32) -> Result<Vec<SearchResult>, KernelError> {\n+        if k == 0 {\n+            return Ok(Vec::new());\n+        }\n+        if query.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        let mut result_ids: Vec<*const u8> = vec![std::ptr::null(); k as usize];\n+        let mut result_id_lens: Vec<u32> = vec![0; k as usize];\n+        let mut result_scores: Vec<f32> = vec![0.0; k as usize];\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: handle is valid, query is a valid slice of correct length.\n+        // Output arrays are correctly sized to hold up to k results.\n+        // The returned ID pointers borrow from the index (valid until mutation).\n+        let rc = unsafe {\n+            zig_embedding_index_search(\n+                self.handle,\n+                query.as_ptr(),\n+                self.dims,\n+                result_ids.as_mut_ptr(),\n+                result_id_lens.as_mut_ptr(),\n+                result_scores.as_mut_ptr(),\n+                k,\n+                &mut written,\n+            )\n+        };\n+\n+        match rc {\n+            0 => {\n+                let results = (0..written as usize)\n+                    .map(|i| {\n+                        // SAFETY: The Zig index returned valid pointers into its\n+                        // own storage. We copy them into owned Strings immediately.\n+                        let id_slice = unsafe {\n+                            std::slice::from_raw_parts(result_ids[i], result_id_lens[i] as usize)\n+                        };\n+                        let id = String::from_utf8_lossy(id_slice).into_owned();\n+                        SearchResult {\n+                            id,\n+                            score: result_scores[i],\n+                        }\n+                    })\n+                    .collect();\n+                Ok(results)\n+            }\n+            -1 => Err(KernelError::InvalidInput),\n+            -2 => {\n+                // k==0 case — we handle this above but the Zig layer also\n+                // returns -2 for k==0. Treat as empty result.\n+                Ok(Vec::new())\n+            }\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Return the number of entries in the index.\n+    pub fn count(&self) -> u32 {\n+        // SAFETY: handle is valid.\n+        let rc = unsafe { zig_embedding_index_count(self.handle) };"
  },
  {
    "id": 2812361168,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 192,
    "body": "## Semgrep Finding: semgrep.markymark.rust.unsafe-block\n\nUnsafe block detected. Require explicit safety justification and review.\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/81)",
    "created_at": "2026-02-16T13:09:06Z",
    "updated_at": "2026-02-16T13:09:06Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };\n+        if handle.is_null() {\n+            return Err(KernelError::InternalError(-3));\n+        }\n+\n+        Ok(Self {\n+            handle,\n+            dims,\n+            _not_send_sync: PhantomData,\n+        })\n+    }\n+\n+    /// Add an embedding to the index with the given ID.\n+    ///\n+    /// Both the ID and vector are copied into the Zig index. If an entry with\n+    /// the same ID already exists, its vector is replaced in-place.\n+    ///\n+    /// Returns `Err(InvalidInput)` if:\n+    /// - `id` is empty\n+    /// - `embedding` length does not match the index dimensionality\n+    pub fn add(&mut self, id: &str, embedding: &[f32]) -> Result<(), KernelError> {\n+        if id.is_empty() {\n+            return Err(KernelError::InvalidInput);\n+        }\n+        if embedding.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: handle is valid (created in new(), not yet destroyed).\n+        // id and embedding are valid slices with correct lengths.\n+        let rc = unsafe {\n+            zig_embedding_index_add(\n+                self.handle,\n+                id.as_ptr(),\n+                id.len() as u32,\n+                embedding.as_ptr(),\n+                self.dims,\n+            )\n+        };\n+\n+        match rc {\n+            0 => Ok(()),\n+            -1 => Err(KernelError::InvalidInput),\n+            -3 => Err(KernelError::InternalError(-3)),\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Search the index for the top-K most similar embeddings to `query`.\n+    ///\n+    /// Results are sorted by descending cosine similarity score.\n+    ///\n+    /// Returns an empty vector if `k` is 0 or the index is empty.\n+    /// Returns `Err(InvalidInput)` if `query` length does not match dimensions.\n+    pub fn search(&self, query: &[f32], k: u32) -> Result<Vec<SearchResult>, KernelError> {\n+        if k == 0 {\n+            return Ok(Vec::new());\n+        }\n+        if query.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        let mut result_ids: Vec<*const u8> = vec![std::ptr::null(); k as usize];\n+        let mut result_id_lens: Vec<u32> = vec![0; k as usize];\n+        let mut result_scores: Vec<f32> = vec![0.0; k as usize];\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: handle is valid, query is a valid slice of correct length.\n+        // Output arrays are correctly sized to hold up to k results.\n+        // The returned ID pointers borrow from the index (valid until mutation).\n+        let rc = unsafe {\n+            zig_embedding_index_search(\n+                self.handle,\n+                query.as_ptr(),\n+                self.dims,\n+                result_ids.as_mut_ptr(),\n+                result_id_lens.as_mut_ptr(),\n+                result_scores.as_mut_ptr(),\n+                k,\n+                &mut written,\n+            )\n+        };\n+\n+        match rc {\n+            0 => {\n+                let results = (0..written as usize)\n+                    .map(|i| {\n+                        // SAFETY: The Zig index returned valid pointers into its\n+                        // own storage. We copy them into owned Strings immediately.\n+                        let id_slice = unsafe {\n+                            std::slice::from_raw_parts(result_ids[i], result_id_lens[i] as usize)\n+                        };"
  },
  {
    "id": 2812361172,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 181,
    "body": "## Semgrep Finding: semgrep.markymark.rust.unsafe-block\n\nUnsafe block detected. Require explicit safety justification and review.\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/80)",
    "created_at": "2026-02-16T13:09:06Z",
    "updated_at": "2026-02-16T13:09:06Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };\n+        if handle.is_null() {\n+            return Err(KernelError::InternalError(-3));\n+        }\n+\n+        Ok(Self {\n+            handle,\n+            dims,\n+            _not_send_sync: PhantomData,\n+        })\n+    }\n+\n+    /// Add an embedding to the index with the given ID.\n+    ///\n+    /// Both the ID and vector are copied into the Zig index. If an entry with\n+    /// the same ID already exists, its vector is replaced in-place.\n+    ///\n+    /// Returns `Err(InvalidInput)` if:\n+    /// - `id` is empty\n+    /// - `embedding` length does not match the index dimensionality\n+    pub fn add(&mut self, id: &str, embedding: &[f32]) -> Result<(), KernelError> {\n+        if id.is_empty() {\n+            return Err(KernelError::InvalidInput);\n+        }\n+        if embedding.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: handle is valid (created in new(), not yet destroyed).\n+        // id and embedding are valid slices with correct lengths.\n+        let rc = unsafe {\n+            zig_embedding_index_add(\n+                self.handle,\n+                id.as_ptr(),\n+                id.len() as u32,\n+                embedding.as_ptr(),\n+                self.dims,\n+            )\n+        };\n+\n+        match rc {\n+            0 => Ok(()),\n+            -1 => Err(KernelError::InvalidInput),\n+            -3 => Err(KernelError::InternalError(-3)),\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Search the index for the top-K most similar embeddings to `query`.\n+    ///\n+    /// Results are sorted by descending cosine similarity score.\n+    ///\n+    /// Returns an empty vector if `k` is 0 or the index is empty.\n+    /// Returns `Err(InvalidInput)` if `query` length does not match dimensions.\n+    pub fn search(&self, query: &[f32], k: u32) -> Result<Vec<SearchResult>, KernelError> {\n+        if k == 0 {\n+            return Ok(Vec::new());\n+        }\n+        if query.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        let mut result_ids: Vec<*const u8> = vec![std::ptr::null(); k as usize];\n+        let mut result_id_lens: Vec<u32> = vec![0; k as usize];\n+        let mut result_scores: Vec<f32> = vec![0.0; k as usize];\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: handle is valid, query is a valid slice of correct length.\n+        // Output arrays are correctly sized to hold up to k results.\n+        // The returned ID pointers borrow from the index (valid until mutation).\n+        let rc = unsafe {\n+            zig_embedding_index_search(\n+                self.handle,\n+                query.as_ptr(),\n+                self.dims,\n+                result_ids.as_mut_ptr(),\n+                result_id_lens.as_mut_ptr(),\n+                result_scores.as_mut_ptr(),\n+                k,\n+                &mut written,\n+            )\n+        };"
  },
  {
    "id": 2812361182,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 137,
    "body": "## Semgrep Finding: semgrep.markymark.rust.unsafe-block\n\nUnsafe block detected. Require explicit safety justification and review.\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/79)",
    "created_at": "2026-02-16T13:09:06Z",
    "updated_at": "2026-02-16T13:09:06Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };\n+        if handle.is_null() {\n+            return Err(KernelError::InternalError(-3));\n+        }\n+\n+        Ok(Self {\n+            handle,\n+            dims,\n+            _not_send_sync: PhantomData,\n+        })\n+    }\n+\n+    /// Add an embedding to the index with the given ID.\n+    ///\n+    /// Both the ID and vector are copied into the Zig index. If an entry with\n+    /// the same ID already exists, its vector is replaced in-place.\n+    ///\n+    /// Returns `Err(InvalidInput)` if:\n+    /// - `id` is empty\n+    /// - `embedding` length does not match the index dimensionality\n+    pub fn add(&mut self, id: &str, embedding: &[f32]) -> Result<(), KernelError> {\n+        if id.is_empty() {\n+            return Err(KernelError::InvalidInput);\n+        }\n+        if embedding.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: handle is valid (created in new(), not yet destroyed).\n+        // id and embedding are valid slices with correct lengths.\n+        let rc = unsafe {\n+            zig_embedding_index_add(\n+                self.handle,\n+                id.as_ptr(),\n+                id.len() as u32,\n+                embedding.as_ptr(),\n+                self.dims,\n+            )\n+        };"
  },
  {
    "id": 2812361186,
    "user": "github-advanced-security[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 98,
    "body": "## Semgrep Finding: semgrep.markymark.rust.unsafe-block\n\nUnsafe block detected. Require explicit safety justification and review.\n\n[Show more details](https://github.com/sethyanow/markymark/security/code-scanning/78)",
    "created_at": "2026-02-16T13:09:06Z",
    "updated_at": "2026-02-16T13:09:06Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };"
  },
  {
    "id": 2812392586,
    "user": "Copilot",
    "path": "zig/src/exports_embed.zig",
    "line": null,
    "body": "The docstring for `zig_embedding_index_search` says it returns \">= 0 — number of results written\", but the implementation always returns 0 on success and communicates the count via the `written` out-parameter. Please either update the docs to reflect the out-parameter contract, or change the function to return `n` on success for consistency with the comment and with typical C-ABI conventions.\n```suggestion\n/// The `written` out-parameter receives the number of results actually written,\n/// which will be <= k.\n///\n/// Returns:\n///   0   — success (number of results written is stored in *written)\n///  -1   — invalid input (null handle/pointers, wrong dims)\n///  -2   — k == 0\n```",
    "created_at": "2026-02-16T13:16:55Z",
    "updated_at": "2026-02-16T13:16:58Z",
    "diff_hunk": "@@ -0,0 +1,370 @@\n+const std = @import(\"std\");\n+const embeddings = @import(\"shared/embeddings.zig\");\n+const EmbeddingIndex = embeddings.EmbeddingIndex;\n+const SearchResult = embeddings.SearchResult;\n+\n+/// Allocator used for embedding index heap allocations.\n+/// page_allocator is the simplest choice for long-lived, FFI-owned memory.\n+const index_allocator = std.heap.page_allocator;\n+\n+/// Create a new embedding index for vectors of the given dimensionality.\n+///\n+/// Returns an opaque handle on success, or null if dims == 0.\n+/// The caller (Rust) owns the handle and MUST call `zig_embedding_index_destroy`\n+/// to free all memory.\n+export fn zig_embedding_index_create(dims: u32) ?*anyopaque {\n+    const idx = index_allocator.create(EmbeddingIndex) catch return null;\n+    idx.* = EmbeddingIndex.init(index_allocator, dims) orelse {\n+        index_allocator.destroy(idx);\n+        return null;\n+    };\n+    return @ptrCast(idx);\n+}\n+\n+/// Destroy an embedding index, freeing all entries and internal storage.\n+///\n+/// After this call the handle is invalid. Passing null is a no-op.\n+export fn zig_embedding_index_destroy(handle: ?*anyopaque) void {\n+    const idx = castHandle(handle) orelse return;\n+    idx.deinit();\n+    index_allocator.destroy(idx);\n+}\n+\n+/// Add an embedding to the index. Both the ID and vector are copied internally.\n+///\n+/// If an entry with the same ID already exists, its vector is replaced.\n+///\n+/// Returns:\n+///   0  — success\n+///  -1  — invalid input (null handle, null pointers, wrong dims, empty id)\n+///  -3  — allocation failure\n+export fn zig_embedding_index_add(\n+    handle: ?*anyopaque,\n+    id: ?[*]const u8,\n+    id_len: u32,\n+    embedding: ?[*]const f32,\n+    dims: u32,\n+) i32 {\n+    const idx = castHandle(handle) orelse return -1;\n+    const id_ptr = id orelse return -1;\n+    const emb_ptr = embedding orelse return -1;\n+    if (id_len == 0) return -1;\n+    if (dims == 0) return -1;\n+\n+    return idx.add(id_ptr[0..id_len], emb_ptr[0..dims]);\n+}\n+\n+/// Search the index for the top-K most similar embeddings to the query.\n+///\n+/// Results are written into caller-provided parallel arrays:\n+///   result_ids[i]      — pointer to the i-th result's ID string (borrowed from index)\n+///   result_id_lens[i]  — length of the i-th result's ID string\n+///   result_scores[i]   — cosine similarity score of the i-th result\n+///\n+/// Results are sorted by descending similarity score.\n+///\n+/// Returns:\n+///   >= 0  — number of results written\n+///  -1     — invalid input (null handle/pointers, wrong dims)\n+///  -2     — k == 0"
  },
  {
    "id": 2812392629,
    "user": "Copilot",
    "path": "zig/src/c_adapter.zig",
    "line": 350,
    "body": "New C-ABI exports here mix prefixes (`zig_*` for similarity/entities but `asm_*` for normalize/quantize). This makes the external API harder to discover and use consistently. Consider renaming these to `zig_normalize_f32_l2` / `zig_quantize_f32_to_q4_0` / `zig_dequantize_q4_0_to_f32` (or otherwise documenting the intended naming scheme) before the ABI becomes relied upon.",
    "created_at": "2026-02-16T13:16:55Z",
    "updated_at": "2026-02-16T13:16:58Z",
    "diff_hunk": "@@ -214,6 +225,129 @@ export fn marky_scan_block_ids(\n     return 0;\n }\n \n+// ============================================================================\n+// Shared kernel exports: similarity, normalize, entities, quantize\n+// ============================================================================\n+\n+/// SIMD-accelerated cosine similarity between two f32 vectors.\n+///\n+/// Returns cosine similarity in [-1.0, 1.0].\n+/// Returns -2.0 on error (null pointers, zero dims, zero-magnitude vector).\n+export fn zig_cosine_similarity(\n+    a: ?[*]const f32,\n+    b: ?[*]const f32,\n+    dims: u32,\n+) f32 {\n+    const va = a orelse return -2.0;\n+    const vb = b orelse return -2.0;\n+    if (dims == 0) return -2.0;\n+    return similarity.cosine_similarity(va, vb, dims);\n+}\n+\n+/// Jaccard similarity between two sorted u32 hash sets.\n+///\n+/// Both sets MUST be sorted in ascending order.\n+/// Returns |intersection| / |union| in [0.0, 1.0].\n+/// Returns -1.0 on error (null pointers).\n+export fn zig_jaccard_similarity(\n+    set1: ?[*]const u32,\n+    set1_len: u32,\n+    set2: ?[*]const u32,\n+    set2_len: u32,\n+) f32 {\n+    const s1 = set1 orelse return -1.0;\n+    const s2 = set2 orelse return -1.0;\n+    return similarity.jaccard_similarity(s1, set1_len, s2, set2_len);\n+}\n+\n+/// SIMD-accelerated entity hash extraction.\n+///\n+/// Scans text for words, produces FNV-1a u32 hash for each.\n+///\n+/// Returns:\n+///   0  — success\n+///  -1  — invalid input (null pointer)\n+///  -2  — buffer too small (writes as many as fit)\n+export fn zig_extract_entity_hashes(\n+    text_ptr: ?[*]const u8,\n+    text_len: u32,\n+    output_ids: ?[*]u32,\n+    capacity: u32,\n+    written: ?*u32,\n+) i32 {\n+    const w = written orelse return -1;\n+    const t = text_ptr orelse {\n+        if (text_len == 0) {\n+            w.* = 0;\n+            return 0;\n+        }\n+        return -1;\n+    };\n+    const o = output_ids orelse return -1;\n+\n+    if (text_len == 0) {\n+        w.* = 0;\n+        return 0;\n+    }\n+    if (capacity == 0) return -2;\n+\n+    return entities.extract_entity_hashes(t, text_len, o, capacity, w);\n+}\n+\n+/// SIMD-accelerated L2 normalization of f32 vector.\n+///\n+/// Produces a unit vector (||output|| == 1.0).\n+///\n+/// Returns:\n+///   0  — success\n+///  -1  — invalid input (null pointer, zero length, zero vector)\n+export fn asm_normalize_f32_l2(\n+    input: ?[*]const f32,\n+    output: ?[*]f32,\n+    n: u32,\n+) i32 {\n+    const i = input orelse return -1;\n+    const o = output orelse return -1;\n+    if (n == 0) return -1;\n+    return normalize.normalize_f32_l2(i, o, n);\n+}\n+\n+/// SIMD-accelerated Q4_0 quantization: f32 -> 4-bit packed format.\n+///\n+/// n must be divisible by 32 (Q4 block size).\n+///\n+/// Returns:\n+///   0  — success\n+///  -1  — invalid input (n not divisible by 32, zero, null pointer)\n+export fn asm_quantize_f32_to_q4_0(\n+    input: ?[*]const f32,\n+    output: ?[*]u8,\n+    n: u32,\n+) i32 {\n+    const i = input orelse return -1;\n+    const o = output orelse return -1;\n+    if (n == 0) return -1;\n+    return quantize_mod.quantize_f32_to_q4_0(i, o, n);\n+}\n+\n+/// SIMD-accelerated Q4_0 dequantization: 4-bit packed format -> f32.\n+///\n+/// n must be divisible by 32 (Q4 block size).\n+///\n+/// Returns:\n+///   0  — success\n+///  -1  — invalid input\n+export fn asm_dequantize_q4_0_to_f32(\n+    input: ?[*]const u8,\n+    output: ?[*]f32,\n+    n: u32,\n+) i32 {\n+    const i = input orelse return -1;\n+    const o = output orelse return -1;\n+    if (n == 0) return -1;\n+    return quantize_mod.dequantize_q4_0_to_f32(i, o, n);\n+}"
  },
  {
    "id": 2812392644,
    "user": "Copilot",
    "path": "markymark-kernels/src/embed.rs",
    "line": 84,
    "body": "`PhantomData<*mut ()>` does not prevent `Send`/`Sync` auto-trait derivation here (raw pointers are `Send`/`Sync`), so `EmbeddingIndex` may still be `Send` and/or `Sync` despite the safety docs. If the type must be !Send + !Sync, use a marker that is explicitly !Send and !Sync (e.g., `PhantomData<std::rc::Rc<()>>`) or add an appropriate non-Send/Sync field, and adjust the comment accordingly.",
    "created_at": "2026-02-16T13:16:55Z",
    "updated_at": "2026-02-16T13:16:58Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}"
  },
  {
    "id": 2812392657,
    "user": "Copilot",
    "path": "zig/src/shared/entities.zig",
    "line": 21,
    "body": "The return-code docs say `-1 — invalid input (zero length)`, but the implementation treats `text_len == 0` as a successful no-op (sets `written` to 0 and returns 0). Please update the doc comment to match the actual behavior (and keep it consistent with the C ABI wrapper which also treats empty text as success).",
    "created_at": "2026-02-16T13:16:55Z",
    "updated_at": "2026-02-16T13:16:58Z",
    "diff_hunk": "@@ -0,0 +1,210 @@\n+const std = @import(\"std\");\n+const ref = @import(\"../reference/entities_ref.zig\");\n+\n+/// SIMD-accelerated entity hash extraction.\n+///\n+/// Scans text for word boundaries using @Vector(16, u8) to detect separators,\n+/// then hashes each word with FNV-1a (lower 32 bits).\n+///\n+/// Returns:\n+///   0  — success\n+///  -1  — invalid input (zero length)\n+///  -2  — buffer too small (writes as many as fit)\n+pub fn extract_entity_hashes(\n+    text: [*]const u8,\n+    text_len: u32,\n+    output_ids: [*]u32,\n+    capacity: u32,\n+    written: *u32,\n+) i32 {\n+    if (text_len == 0) {\n+        written.* = 0;\n+        return 0;"
  },
  {
    "id": 2812392680,
    "user": "Copilot",
    "path": "zig/src/reference/similarity_ref.zig",
    "line": 50,
    "body": "The Jaccard docs mention returning -1.0 on null pointers, but the function doesn’t check for null before slicing `set1[0..set1_len]` / `set2[0..set2_len]`. Please either implement the null checks to match the documented contract, or remove the null-pointer return code from the docs and rely on the optional-pointer checks in the exported C ABI wrapper.",
    "created_at": "2026-02-16T13:16:56Z",
    "updated_at": "2026-02-16T13:16:58Z",
    "diff_hunk": "@@ -0,0 +1,143 @@\n+const std = @import(\"std\");\n+const math = std.math;\n+\n+/// Scalar cosine similarity between two f32 vectors.\n+///\n+/// Returns the cosine of the angle between vectors a and b:\n+///   dot(a,b) / (||a|| * ||b||)\n+///\n+/// Returns -2.0 on error (null pointers, zero length, zero-magnitude vector).\n+pub fn cosine_similarity(a: [*]const f32, b: [*]const f32, dims: u32) f32 {\n+    if (dims == 0) return -2.0;\n+\n+    const va = a[0..dims];\n+    const vb = b[0..dims];\n+\n+    var dot: f64 = 0.0;\n+    var norm_a: f64 = 0.0;\n+    var norm_b: f64 = 0.0;\n+\n+    for (va, vb) |ai, bi| {\n+        const fa: f64 = @floatCast(ai);\n+        const fb: f64 = @floatCast(bi);\n+        dot += fa * fb;\n+        norm_a += fa * fa;\n+        norm_b += fb * fb;\n+    }\n+\n+    const mag = @sqrt(norm_a) * @sqrt(norm_b);\n+    if (mag < 1e-30) return -2.0; // zero-magnitude vector\n+\n+    return @floatCast(dot / mag);\n+}\n+\n+/// Scalar Jaccard similarity between two sorted u32 hash sets.\n+///\n+/// Both sets MUST be sorted in ascending order.\n+/// Returns |intersection| / |union| as f32.\n+///\n+/// Returns -1.0 on error (null pointers).\n+/// Returns 0.0 for empty sets (|union| == 0).\n+/// Returns 1.0 for identical sets.\n+pub fn jaccard_similarity(set1: [*]const u32, set1_len: u32, set2: [*]const u32, set2_len: u32) f32 {\n+    if (set1_len == 0 and set2_len == 0) return 0.0;\n+    if (set1_len == 0 or set2_len == 0) return 0.0;\n+\n+    const s1 = set1[0..set1_len];\n+    const s2 = set2[0..set2_len];\n+"
  },
  {
    "id": 2812392701,
    "user": "Copilot",
    "path": "zig/src/c_adapter.zig",
    "line": null,
    "body": "`zig_extract_entity_hashes` has inconsistent zero-length handling: when `text_len == 0` it should be a no-op regardless of `text_ptr`, `output_ids`, or `capacity`, but currently a non-null `text_ptr` with `text_len == 0` still requires `output_ids` to be non-null. Also, when `capacity == 0` the function returns -2 without setting `written` to 0, which breaks the \"writes as many as fit\" contract. Consider returning early for `text_len == 0` before validating `output_ids/capacity`, and ensure `written` is set to 0 on the `capacity == 0` error path.\n```suggestion\n\n    // For zero-length input, this is a no-op regardless of text_ptr, output_ids, or capacity.\n    if (text_len == 0) {\n        w.* = 0;\n        return 0;\n    }\n\n    const t = text_ptr orelse return -1;\n    const o = output_ids orelse return -1;\n\n    if (capacity == 0) {\n        w.* = 0;\n        return -2;\n    }\n```",
    "created_at": "2026-02-16T13:16:56Z",
    "updated_at": "2026-02-16T13:16:58Z",
    "diff_hunk": "@@ -214,6 +225,129 @@ export fn marky_scan_block_ids(\n     return 0;\n }\n \n+// ============================================================================\n+// Shared kernel exports: similarity, normalize, entities, quantize\n+// ============================================================================\n+\n+/// SIMD-accelerated cosine similarity between two f32 vectors.\n+///\n+/// Returns cosine similarity in [-1.0, 1.0].\n+/// Returns -2.0 on error (null pointers, zero dims, zero-magnitude vector).\n+export fn zig_cosine_similarity(\n+    a: ?[*]const f32,\n+    b: ?[*]const f32,\n+    dims: u32,\n+) f32 {\n+    const va = a orelse return -2.0;\n+    const vb = b orelse return -2.0;\n+    if (dims == 0) return -2.0;\n+    return similarity.cosine_similarity(va, vb, dims);\n+}\n+\n+/// Jaccard similarity between two sorted u32 hash sets.\n+///\n+/// Both sets MUST be sorted in ascending order.\n+/// Returns |intersection| / |union| in [0.0, 1.0].\n+/// Returns -1.0 on error (null pointers).\n+export fn zig_jaccard_similarity(\n+    set1: ?[*]const u32,\n+    set1_len: u32,\n+    set2: ?[*]const u32,\n+    set2_len: u32,\n+) f32 {\n+    const s1 = set1 orelse return -1.0;\n+    const s2 = set2 orelse return -1.0;\n+    return similarity.jaccard_similarity(s1, set1_len, s2, set2_len);\n+}\n+\n+/// SIMD-accelerated entity hash extraction.\n+///\n+/// Scans text for words, produces FNV-1a u32 hash for each.\n+///\n+/// Returns:\n+///   0  — success\n+///  -1  — invalid input (null pointer)\n+///  -2  — buffer too small (writes as many as fit)\n+export fn zig_extract_entity_hashes(\n+    text_ptr: ?[*]const u8,\n+    text_len: u32,\n+    output_ids: ?[*]u32,\n+    capacity: u32,\n+    written: ?*u32,\n+) i32 {\n+    const w = written orelse return -1;\n+    const t = text_ptr orelse {\n+        if (text_len == 0) {\n+            w.* = 0;\n+            return 0;\n+        }\n+        return -1;\n+    };\n+    const o = output_ids orelse return -1;\n+\n+    if (text_len == 0) {\n+        w.* = 0;\n+        return 0;\n+    }\n+    if (capacity == 0) return -2;\n+"
  },
  {
    "id": 2812392728,
    "user": "Copilot",
    "path": "markymark-kernels/src/hash.rs",
    "line": 45,
    "body": "This module used to expose `content_hash` via the Zig `marky_content_hash` export, but now it only exposes `extract_entity_hashes`. Since `marky_content_hash` is still exported from the Zig library, this is an API-breaking removal on the Rust side and also diverges from the PR description mentioning content hashing wrappers. Consider reintroducing `content_hash` (possibly in `tokens` or a dedicated module) and placing entity hashing in a more specific module name (or keep both APIs here) to avoid breaking downstream callers.",
    "created_at": "2026-02-16T13:16:56Z",
    "updated_at": "2026-02-16T13:16:58Z",
    "diff_hunk": "@@ -1,62 +1,147 @@\n-//! Content hashing (FNV-1a 64-bit).\n+//! Entity hash extraction (FNV-1a word hashing).\n //!\n-//! Wraps the Zig `marky_content_hash` FFI function for computing\n-//! deterministic content fingerprints.\n+//! Wraps the Zig `zig_extract_entity_hashes` FFI function which tokenizes\n+//! text on whitespace/punctuation and produces an FNV-1a u32 hash per word.\n+\n+use crate::scan::KernelError;\n+\n+/// Maximum number of retry attempts when the output buffer is too small.\n+const MAX_RETRIES: u32 = 3;\n+\n+/// Initial buffer capacity for entity hash results.\n+const INITIAL_CAP: usize = 64;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n \n extern \"C\" {\n-    fn marky_content_hash(text: *const u8, len: u32) -> u64;\n+    fn zig_extract_entity_hashes(\n+        text_ptr: *const u8,\n+        text_len: u32,\n+        output_ids: *mut u32,\n+        capacity: u32,\n+        written: *mut u32,\n+    ) -> i32;\n }\n \n-/// FNV offset basis (hash of empty input).\n-pub const FNV_OFFSET_BASIS: u64 = 0xcbf29ce484222325;\n+// ---------------------------------------------------------------------------\n+// Public API\n+// ---------------------------------------------------------------------------\n \n-/// Compute a deterministic FNV-1a 64-bit hash of the given text.\n+/// Extract entity hashes from text using FNV-1a word hashing.\n+///\n+/// Tokenizes text on whitespace and punctuation boundaries, then hashes each\n+/// word with FNV-1a to produce a truncated u32 hash per token. Useful for\n+/// entity-based similarity comparisons (e.g. Jaccard on hash sets).\n ///\n-/// Returns [`FNV_OFFSET_BASIS`] for empty input (the standard FNV-1a\n-/// offset basis). Useful for content deduplication and change detection.\n-pub fn content_hash(text: &str) -> u64 {\n+/// Returns an empty vector for empty input (not an error).\n+pub fn extract_entity_hashes(text: &str) -> Result<Vec<u32>, KernelError> {\n     if text.is_empty() {\n-        return FNV_OFFSET_BASIS;\n+        return Ok(Vec::new());\n     }\n-    // SAFETY: text.as_ptr() is valid for text.len() bytes.\n-    // marky_content_hash is a pure function with no side effects.\n-    unsafe { marky_content_hash(text.as_ptr(), text.len() as u32) }\n+\n+    let mut buf: Vec<u32> = vec![0; INITIAL_CAP];\n+    let text_bytes = text.as_bytes();"
  },
  {
    "id": 2812392751,
    "user": "Copilot",
    "path": "zig/src/reference/entities_ref.zig",
    "line": 37,
    "body": "The doc comment states `-1 — invalid input (null pointer, zero length)`, but this function returns 0 on `text_len == 0` (no-op success). Please adjust the documented return codes to reflect the actual behavior so callers and parity tests don’t rely on the wrong contract.",
    "created_at": "2026-02-16T13:16:56Z",
    "updated_at": "2026-02-16T13:16:58Z",
    "diff_hunk": "@@ -0,0 +1,146 @@\n+const std = @import(\"std\");\n+\n+/// FNV-1a 64-bit offset basis\n+pub const FNV_OFFSET_BASIS: u64 = 14695981039346656037;\n+/// FNV-1a 64-bit prime\n+pub const FNV_PRIME: u64 = 1099511628211;\n+\n+/// FNV-1a 64-bit hash of a byte slice.\n+pub fn fnv1a_hash(data: []const u8) u64 {\n+    var hash: u64 = FNV_OFFSET_BASIS;\n+    for (data) |byte| {\n+        hash ^= @intCast(byte);\n+        hash *%= FNV_PRIME;\n+    }\n+    return hash;\n+}\n+\n+/// Scalar entity hash extraction.\n+///\n+/// Scans text for word boundaries and produces a FNV-1a hash for each word.\n+/// Words are separated by whitespace or punctuation.\n+/// Output hashes are written as u32 (lower 32 bits of FNV-1a 64-bit).\n+///\n+/// Returns:\n+///   0  — success\n+///  -1  — invalid input (null pointer, zero length)\n+///  -2  — buffer too small (more entities than capacity; writes as many as fit)\n+pub fn extract_entity_hashes(\n+    text: [*]const u8,\n+    text_len: u32,\n+    output_ids: [*]u32,\n+    capacity: u32,\n+    written: *u32,\n+) i32 {\n+    if (text_len == 0) {\n+        written.* = 0;\n+        return 0;\n+    }"
  },
  {
    "id": 2812392778,
    "user": "Copilot",
    "path": "zig/src/reference/similarity_ref.zig",
    "line": 15,
    "body": "The docstring says this returns -2.0 on \"null pointers\", but the implementation immediately slices `a[0..dims]` / `b[0..dims]` without checking for null, which will trap if a null C pointer is passed. Either add an explicit null-pointer check (e.g., via `@intFromPtr`) or update the docs to state that pointers must be non-null and are validated at the C-ABI boundary.",
    "created_at": "2026-02-16T13:16:57Z",
    "updated_at": "2026-02-16T13:16:57Z",
    "diff_hunk": "@@ -0,0 +1,143 @@\n+const std = @import(\"std\");\n+const math = std.math;\n+\n+/// Scalar cosine similarity between two f32 vectors.\n+///\n+/// Returns the cosine of the angle between vectors a and b:\n+///   dot(a,b) / (||a|| * ||b||)\n+///\n+/// Returns -2.0 on error (null pointers, zero length, zero-magnitude vector).\n+pub fn cosine_similarity(a: [*]const f32, b: [*]const f32, dims: u32) f32 {\n+    if (dims == 0) return -2.0;\n+\n+    const va = a[0..dims];\n+    const vb = b[0..dims];"
  },
  {
    "id": 2812395895,
    "user": "coderabbitai[bot]",
    "path": "markymark-kernels/src/embed.rs",
    "line": 210,
    "body": "_⚠️ Potential issue_ | _🟡 Minor_\n\n**Align validation order and guard `written` from FFI.**\n\n`k == 0` currently short-circuits before validating the query length, which contradicts the doc comment. Also, the code trusts `written` from FFI; if it exceeds `k`, indexing will panic. Validate query length first and clamp (or error) on oversized `written`.\n\n<details>\n<summary>✅ Proposed fix</summary>\n\n```diff\n pub fn search(&self, query: &[f32], k: u32) -> Result<Vec<SearchResult>, KernelError> {\n-        if k == 0 {\n-            return Ok(Vec::new());\n-        }\n         if query.len() != self.dims as usize {\n             return Err(KernelError::InvalidInput);\n         }\n+        if k == 0 {\n+            return Ok(Vec::new());\n+        }\n@@\n-            0 => {\n-                let results = (0..written as usize)\n+            0 => {\n+                let written = written.min(k) as usize;\n+                let results = (0..written)\n```\n</details>\n\n<details>\n<summary>🧰 Tools</summary>\n\n<details>\n<summary>🪛 GitHub Check: Semgrep OSS</summary>\n\n[notice] 167-178: Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\nDetected 'unsafe' usage, please audit for secure usage\n\n---\n\n[notice] 186-188: Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\nDetected 'unsafe' usage, please audit for secure usage\n\n---\n\n[warning] 186-188: Semgrep Finding: semgrep.markymark.rust.unsafe-block\nUnsafe block detected. Require explicit safety justification and review.\n\n---\n\n[warning] 167-178: Semgrep Finding: semgrep.markymark.rust.unsafe-block\nUnsafe block detected. Require explicit safety justification and review.\n\n</details>\n\n</details>\n\n<details>\n<summary>🤖 Prompt for AI Agents</summary>\n\n```\nIn `@markymark-kernels/src/embed.rs` around lines 145 - 206, The search method\ncurrently returns early for k==0 before validating query length and trusts the\nFFI-written value; change the order to validate query length (check query.len()\n== self.dims as usize) before handling k==0, then call\nzig_embedding_index_search as before but guard the FFI-written count: after the\ncall, check if written as usize > k as usize and either clamp written = k or\nreturn an error (e.g., KernelError::InternalError) to avoid out-of-bounds\nindexing into result_ids/result_scores; update references to written when\nbuilding results accordingly so you never index beyond k (symbols: search,\nzig_embedding_index_search, written, result_ids, result_id_lens, result_scores).\n```\n\n</details>\n\n<!-- fingerprinting:phantom:medusa:eagle -->\n\n<!-- This is an auto-generated comment by CodeRabbit -->",
    "created_at": "2026-02-16T13:17:39Z",
    "updated_at": "2026-02-16T13:17:41Z",
    "diff_hunk": "@@ -1,4 +1,383 @@\n-//! Embedding index operations.\n+//! Embedding index operations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_embedding_index_*` FFI functions\n-//! for creating, populating, and searching embedding indices.\n+//! Wraps the Zig `zig_embedding_index_*` FFI functions for creating,\n+//! populating, and searching embedding indices. The [`EmbeddingIndex`] struct\n+//! provides a safe Rust API with automatic cleanup via [`Drop`].\n+//!\n+//! # Thread Safety\n+//!\n+//! [`EmbeddingIndex`] is **not** `Send` or `Sync`. The underlying Zig\n+//! allocator and data structures are not thread-safe. Use one index per\n+//! thread, or wrap in appropriate synchronization.\n+\n+use std::marker::PhantomData;\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_embedding_index_create(dims: u32) -> *mut std::ffi::c_void;\n+    fn zig_embedding_index_destroy(handle: *mut std::ffi::c_void);\n+    fn zig_embedding_index_add(\n+        handle: *mut std::ffi::c_void,\n+        id: *const u8,\n+        id_len: u32,\n+        embedding: *const f32,\n+        dims: u32,\n+    ) -> i32;\n+    fn zig_embedding_index_search(\n+        handle: *mut std::ffi::c_void,\n+        query: *const f32,\n+        dims: u32,\n+        result_ids: *mut *const u8,\n+        result_id_lens: *mut u32,\n+        result_scores: *mut f32,\n+        k: u32,\n+        written: *mut u32,\n+    ) -> i32;\n+    fn zig_embedding_index_count(handle: *mut std::ffi::c_void) -> i32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Search result type\n+// ---------------------------------------------------------------------------\n+\n+/// A single search result from an embedding index query.\n+#[derive(Debug, Clone, PartialEq)]\n+pub struct SearchResult {\n+    /// The ID string of the matching entry.\n+    pub id: String,\n+    /// The cosine similarity score (higher = more similar).\n+    pub score: f32,\n+}\n+\n+// ---------------------------------------------------------------------------\n+// EmbeddingIndex\n+// ---------------------------------------------------------------------------\n+\n+/// An in-memory embedding index backed by Zig SIMD kernels.\n+///\n+/// Supports adding named embeddings and performing top-K cosine similarity\n+/// search. The index manages its own memory via an opaque Zig handle.\n+///\n+/// # Examples\n+///\n+/// ```no_run\n+/// use markymark_kernels::embed::EmbeddingIndex;\n+///\n+/// let mut idx = EmbeddingIndex::new(4).unwrap();\n+/// idx.add(\"doc1\", &[1.0, 0.0, 0.0, 0.0]).unwrap();\n+/// idx.add(\"doc2\", &[0.0, 1.0, 0.0, 0.0]).unwrap();\n+///\n+/// let results = idx.search(&[1.0, 0.0, 0.0, 0.0], 2).unwrap();\n+/// assert_eq!(results[0].id, \"doc1\");\n+/// ```\n+pub struct EmbeddingIndex {\n+    handle: *mut std::ffi::c_void,\n+    dims: u32,\n+    /// Prevents Send and Sync — raw pointers are !Send + !Sync by default,\n+    /// but PhantomData<*mut ()> makes the intent explicit and documenting.\n+    _not_send_sync: PhantomData<*mut ()>,\n+}\n+\n+impl EmbeddingIndex {\n+    /// Create a new embedding index for vectors of the given dimensionality.\n+    ///\n+    /// Returns `Err(InvalidInput)` if `dims` is 0.\n+    pub fn new(dims: u32) -> Result<Self, KernelError> {\n+        if dims == 0 {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: zig_embedding_index_create is a pure allocation function.\n+        // Returns null on failure (dims==0 or allocation error).\n+        let handle = unsafe { zig_embedding_index_create(dims) };\n+        if handle.is_null() {\n+            return Err(KernelError::InternalError(-3));\n+        }\n+\n+        Ok(Self {\n+            handle,\n+            dims,\n+            _not_send_sync: PhantomData,\n+        })\n+    }\n+\n+    /// Add an embedding to the index with the given ID.\n+    ///\n+    /// Both the ID and vector are copied into the Zig index. If an entry with\n+    /// the same ID already exists, its vector is replaced in-place.\n+    ///\n+    /// Returns `Err(InvalidInput)` if:\n+    /// - `id` is empty\n+    /// - `embedding` length does not match the index dimensionality\n+    pub fn add(&mut self, id: &str, embedding: &[f32]) -> Result<(), KernelError> {\n+        if id.is_empty() {\n+            return Err(KernelError::InvalidInput);\n+        }\n+        if embedding.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        // SAFETY: handle is valid (created in new(), not yet destroyed).\n+        // id and embedding are valid slices with correct lengths.\n+        let rc = unsafe {\n+            zig_embedding_index_add(\n+                self.handle,\n+                id.as_ptr(),\n+                id.len() as u32,\n+                embedding.as_ptr(),\n+                self.dims,\n+            )\n+        };\n+\n+        match rc {\n+            0 => Ok(()),\n+            -1 => Err(KernelError::InvalidInput),\n+            -3 => Err(KernelError::InternalError(-3)),\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    /// Search the index for the top-K most similar embeddings to `query`.\n+    ///\n+    /// Results are sorted by descending cosine similarity score.\n+    ///\n+    /// Returns an empty vector if `k` is 0 or the index is empty.\n+    /// Returns `Err(InvalidInput)` if `query` length does not match dimensions.\n+    pub fn search(&self, query: &[f32], k: u32) -> Result<Vec<SearchResult>, KernelError> {\n+        if k == 0 {\n+            return Ok(Vec::new());\n+        }\n+        if query.len() != self.dims as usize {\n+            return Err(KernelError::InvalidInput);\n+        }\n+\n+        let mut result_ids: Vec<*const u8> = vec![std::ptr::null(); k as usize];\n+        let mut result_id_lens: Vec<u32> = vec![0; k as usize];\n+        let mut result_scores: Vec<f32> = vec![0.0; k as usize];\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: handle is valid, query is a valid slice of correct length.\n+        // Output arrays are correctly sized to hold up to k results.\n+        // The returned ID pointers borrow from the index (valid until mutation).\n+        let rc = unsafe {\n+            zig_embedding_index_search(\n+                self.handle,\n+                query.as_ptr(),\n+                self.dims,\n+                result_ids.as_mut_ptr(),\n+                result_id_lens.as_mut_ptr(),\n+                result_scores.as_mut_ptr(),\n+                k,\n+                &mut written,\n+            )\n+        };\n+\n+        match rc {\n+            0 => {\n+                let results = (0..written as usize)\n+                    .map(|i| {\n+                        // SAFETY: The Zig index returned valid pointers into its\n+                        // own storage. We copy them into owned Strings immediately.\n+                        let id_slice = unsafe {\n+                            std::slice::from_raw_parts(result_ids[i], result_id_lens[i] as usize)\n+                        };\n+                        let id = String::from_utf8_lossy(id_slice).into_owned();\n+                        SearchResult {\n+                            id,\n+                            score: result_scores[i],\n+                        }\n+                    })\n+                    .collect();\n+                Ok(results)\n+            }\n+            -1 => Err(KernelError::InvalidInput),\n+            -2 => {\n+                // k==0 case — we handle this above but the Zig layer also\n+                // returns -2 for k==0. Treat as empty result.\n+                Ok(Vec::new())\n+            }\n+            other => Err(KernelError::InternalError(other)),\n+        }\n+    }"
  },
  {
    "id": 2812395906,
    "user": "coderabbitai[bot]",
    "path": "markymark-kernels/src/hash.rs",
    "line": 75,
    "body": "_⚠️ Potential issue_ | _🟡 Minor_\n\n<details>\n<summary>🧩 Analysis chain</summary>\n\n🏁 Script executed:\n\n```shell\n# Find the Zig implementation of the FFI function\nfind . -name \"*.zig\" -type f | head -20\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 716\n\n---\n\n🏁 Script executed:\n\n```shell\n# Check if there's documentation about FFI in rust_agent_docs\nfind ./docs -name \"*.md\" -o -name \"*.rs\" | grep -E \"(rust_agent|rust_guidelines)\" | head -20\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 905\n\n---\n\n🏁 Script executed:\n\n```shell\n# Get the full hash.rs file to see tests and complete context\nwc -l markymark-kernels/src/hash.rs\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 96\n\n---\n\n🏁 Script executed:\n\n```shell\n# Look for the Zig implementation of zig_extract_entity_hashes\nrg -n \"zig_extract_entity_hashes|extract_entity_hashes\" --type zig\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 2866\n\n---\n\n🏁 Script executed:\n\n```shell\n# Check the complete hash.rs file\ncat -n markymark-kernels/src/hash.rs\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 5913\n\n---\n\n🏁 Script executed:\n\n```shell\n# Check the Zig FFI implementation and tests\nsed -n '271,310p' zig/src/c_adapter.zig\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 1001\n\n---\n\n🏁 Script executed:\n\n```shell\n# Check the Zig tests for buffer overflow behavior\nsed -n '691,730p' zig/src/c_adapter.zig\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 1512\n\n---\n\n🏁 Script executed:\n\n```shell\n# Read the FFI documentation from rust_agent_docs\ncat docs/rust_agent_docs/advanced/ffi.md\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 4927\n\n---\n\n\n\n</details>\n\n**Clarify retry semantics and add FFI contract validation.**\n\nThe loop `for _ in 0..=MAX_RETRIES` where `MAX_RETRIES = 3` performs 4 total iterations. If the intent is \"initial attempt + up to 3 retries,\" consider renaming `MAX_RETRIES` to `MAX_ATTEMPTS` or `MAX_RETRY_ATTEMPTS` for clarity, as the current name could be misread as \"3 total attempts.\"\n\nAdditionally, add a defensive check that `written <= capacity` after the FFI call—the Zig contract guarantees this, but validation guards against FFI contract violations. Optionally, guard `text.len()` before casting to `u32` (line 47) to prevent silent truncation on hypothetical inputs >4GB, though this is unlikely in practice.\n\n<details>\n<summary>🧰 Tools</summary>\n\n<details>\n<summary>🪛 GitHub Check: Semgrep OSS</summary>\n\n[notice] 55-57: Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\nDetected 'unsafe' usage, please audit for secure usage\n\n---\n\n[warning] 55-57: Semgrep Finding: semgrep.markymark.rust.unsafe-block\nUnsafe block detected. Require explicit safety justification and review.\n\n</details>\n\n</details>\n\n<details>\n<summary>🤖 Prompt for AI Agents</summary>\n\n```\nIn `@markymark-kernels/src/hash.rs` around lines 8 - 74, The retry loop semantics\nare unclear and lack FFI validation; rename MAX_RETRIES to MAX_ATTEMPTS (or\nMAX_RETRY_ATTEMPTS) and change the loop so it does an initial attempt plus up to\nN retries (e.g., for attempt in 0..=MAX_ATTEMPTS or while attempts <\nMAX_ATTEMPTS) to match the intended \"1 + retries\" behavior in\nextract_entity_hashes, add a defensive check after calling\nzig_extract_entity_hashes that validates written <= cap and return\nKernelError::InternalError if violated, and guard the cast of text_bytes.len()\nto u32 by checking for overflow (return an appropriate KernelError) before\nassigning text_len to prevent silent truncation for inputs > u32::MAX; reference\nsymbols: MAX_RETRIES/MAX_ATTEMPTS, INITIAL_CAP, extract_entity_hashes,\nzig_extract_entity_hashes, written, cap, KernelError.\n```\n\n</details>\n\n<!-- fingerprinting:phantom:medusa:eagle -->\n\n<!-- This is an auto-generated comment by CodeRabbit -->",
    "created_at": "2026-02-16T13:17:39Z",
    "updated_at": "2026-02-16T13:17:41Z",
    "diff_hunk": "@@ -1,62 +1,147 @@\n-//! Content hashing (FNV-1a 64-bit).\n+//! Entity hash extraction (FNV-1a word hashing).\n //!\n-//! Wraps the Zig `marky_content_hash` FFI function for computing\n-//! deterministic content fingerprints.\n+//! Wraps the Zig `zig_extract_entity_hashes` FFI function which tokenizes\n+//! text on whitespace/punctuation and produces an FNV-1a u32 hash per word.\n+\n+use crate::scan::KernelError;\n+\n+/// Maximum number of retry attempts when the output buffer is too small.\n+const MAX_RETRIES: u32 = 3;\n+\n+/// Initial buffer capacity for entity hash results.\n+const INITIAL_CAP: usize = 64;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n \n extern \"C\" {\n-    fn marky_content_hash(text: *const u8, len: u32) -> u64;\n+    fn zig_extract_entity_hashes(\n+        text_ptr: *const u8,\n+        text_len: u32,\n+        output_ids: *mut u32,\n+        capacity: u32,\n+        written: *mut u32,\n+    ) -> i32;\n }\n \n-/// FNV offset basis (hash of empty input).\n-pub const FNV_OFFSET_BASIS: u64 = 0xcbf29ce484222325;\n+// ---------------------------------------------------------------------------\n+// Public API\n+// ---------------------------------------------------------------------------\n \n-/// Compute a deterministic FNV-1a 64-bit hash of the given text.\n+/// Extract entity hashes from text using FNV-1a word hashing.\n+///\n+/// Tokenizes text on whitespace and punctuation boundaries, then hashes each\n+/// word with FNV-1a to produce a truncated u32 hash per token. Useful for\n+/// entity-based similarity comparisons (e.g. Jaccard on hash sets).\n ///\n-/// Returns [`FNV_OFFSET_BASIS`] for empty input (the standard FNV-1a\n-/// offset basis). Useful for content deduplication and change detection.\n-pub fn content_hash(text: &str) -> u64 {\n+/// Returns an empty vector for empty input (not an error).\n+pub fn extract_entity_hashes(text: &str) -> Result<Vec<u32>, KernelError> {\n     if text.is_empty() {\n-        return FNV_OFFSET_BASIS;\n+        return Ok(Vec::new());\n     }\n-    // SAFETY: text.as_ptr() is valid for text.len() bytes.\n-    // marky_content_hash is a pure function with no side effects.\n-    unsafe { marky_content_hash(text.as_ptr(), text.len() as u32) }\n+\n+    let mut buf: Vec<u32> = vec![0; INITIAL_CAP];\n+    let text_bytes = text.as_bytes();\n+    let text_ptr = text_bytes.as_ptr();\n+    let text_len = text_bytes.len() as u32;\n+\n+    for _ in 0..=MAX_RETRIES {\n+        let cap = buf.len() as u32;\n+        let mut written: u32 = 0;\n+\n+        // SAFETY: text_ptr valid for text_len bytes, buf has capacity cap,\n+        // written is a valid mutable reference. FFI writes at most cap u32s.\n+        let rc = unsafe {\n+            zig_extract_entity_hashes(text_ptr, text_len, buf.as_mut_ptr(), cap, &mut written)\n+        };\n+\n+        match rc {\n+            0 => {\n+                buf.truncate(written as usize);\n+                return Ok(buf);\n+            }\n+            -1 => return Err(KernelError::InvalidInput),\n+            -2 => {\n+                // Double capacity and retry\n+                let new_cap = (buf.len() * 2).max(INITIAL_CAP);\n+                buf.resize(new_cap, 0);\n+            }\n+            other => return Err(KernelError::InternalError(other)),\n+        }\n+    }\n+\n+    Err(KernelError::BufferTooSmall)"
  },
  {
    "id": 2812395918,
    "user": "coderabbitai[bot]",
    "path": "markymark-kernels/src/similarity.rs",
    "line": 84,
    "body": "_⚠️ Potential issue_ | _🟡 Minor_\n\n<details>\n<summary>🧩 Analysis chain</summary>\n\n🏁 Script executed:\n\n```shell\nfind . -type f -name \"similarity.rs\" | head -20\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 100\n\n---\n\n🏁 Script executed:\n\n```shell\nwc -l markymark-kernels/src/similarity.rs\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 102\n\n---\n\n🏁 Script executed:\n\n```shell\nsed -n '1,100p' markymark-kernels/src/similarity.rs | cat -n\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 4010\n\n---\n\n🏁 Script executed:\n\n```shell\nrg \"enum KernelError\" -A 10\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 769\n\n---\n\n🏁 Script executed:\n\n```shell\nrg \"zig_cosine_similarity|zig_jaccard_similarity\" -B 2 -A 2\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 6271\n\n---\n\n\n\n</details>\n\n**Guard against usize→u32 truncation before FFI calls.**\n\nPublic APIs accept slices; if a length exceeds `u32::MAX`, the `as u32` cast silently truncates and yields incorrect results. Add a fallible conversion to keep behavior well-defined.\n\n<details>\n<summary>🔧 Suggested fix</summary>\n\n```diff\n pub fn cosine_similarity(a: &[f32], b: &[f32]) -> Result<f32, KernelError> {\n     if a.is_empty() || b.is_empty() {\n         return Err(KernelError::InvalidInput);\n     }\n     if a.len() != b.len() {\n         return Err(KernelError::InvalidInput);\n     }\n+    let dims = u32::try_from(a.len()).map_err(|_| KernelError::InvalidInput)?;\n \n     // SAFETY: a and b are valid slices with matching lengths.\n     // zig_cosine_similarity returns -2.0 on null pointer (cannot happen here)\n     // or on dims==0 (checked above).\n-    let result = unsafe { zig_cosine_similarity(a.as_ptr(), b.as_ptr(), a.len() as u32) };\n+    let result = unsafe { zig_cosine_similarity(a.as_ptr(), b.as_ptr(), dims) };\n```\n\n```diff\n pub fn jaccard_similarity(set1: &[u32], set2: &[u32]) -> Result<f32, KernelError> {\n     // Both empty → 0.0 (Zig returns 0.0 for empty sets)\n     if set1.is_empty() && set2.is_empty() {\n         return Ok(0.0);\n     }\n \n     // SAFETY: set1 and set2 are valid slices. zig_jaccard_similarity returns\n     // -1.0 on null pointer (cannot happen with valid slice pointers).\n+    let set1_len = u32::try_from(set1.len()).map_err(|_| KernelError::InvalidInput)?;\n+    let set2_len = u32::try_from(set2.len()).map_err(|_| KernelError::InvalidInput)?;\n     let result = unsafe {\n         zig_jaccard_similarity(\n             set1.as_ptr(),\n-            set1.len() as u32,\n+            set1_len,\n             set2.as_ptr(),\n-            set2.len() as u32,\n+            set2_len,\n         )\n     };\n```\n</details>\n\n<details>\n<summary>🧰 Tools</summary>\n\n<details>\n<summary>🪛 GitHub Check: Semgrep OSS</summary>\n\n[notice] 43-43: Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\nDetected 'unsafe' usage, please audit for secure usage\n\n---\n\n[notice] 68-75: Semgrep Finding: rust.lang.security.unsafe-usage.unsafe-usage\nDetected 'unsafe' usage, please audit for secure usage\n\n---\n\n[warning] 68-75: Semgrep Finding: semgrep.markymark.rust.unsafe-block\nUnsafe block detected. Require explicit safety justification and review.\n\n---\n\n[warning] 43-43: Semgrep Finding: semgrep.markymark.rust.unsafe-block\nUnsafe block detected. Require explicit safety justification and review.\n\n</details>\n\n</details>\n\n<details>\n<summary>🤖 Prompt for AI Agents</summary>\n\n```\nIn `@markymark-kernels/src/similarity.rs` around lines 32 - 82, The slice length\ncasts to u32 in cosine_similarity (a.len() as u32) and jaccard_similarity\n(set1.len() as u32, set2.len() as u32) can silently truncate for lengths >\nu32::MAX; add a guard that converts lengths with a fallible check (e.g.\nu32::try_from(len).map_err(|_| KernelError::InvalidInput) or compare len() <=\nu32::MAX as usize) and return KernelError::InvalidInput if conversion fails\nbefore making the unsafe zig_* FFI calls so you never pass truncated lengths to\nzig_cosine_similarity or zig_jaccard_similarity.\n```\n\n</details>\n\n<!-- fingerprinting:phantom:medusa:eagle -->\n\n<!-- This is an auto-generated comment by CodeRabbit -->",
    "created_at": "2026-02-16T13:17:39Z",
    "updated_at": "2026-02-16T13:17:41Z",
    "diff_hunk": "@@ -1,4 +1,189 @@\n-//! Similarity computations.\n+//! Similarity computations via Zig SIMD kernels.\n //!\n-//! This module will wrap the Zig `zig_cosine_similarity` and\n-//! `zig_jaccard_similarity` FFI functions.\n+//! Wraps the Zig `zig_cosine_similarity` and `zig_jaccard_similarity` FFI\n+//! functions exported from `c_adapter.zig`.\n+\n+use crate::scan::KernelError;\n+\n+// ---------------------------------------------------------------------------\n+// FFI declarations\n+// ---------------------------------------------------------------------------\n+\n+extern \"C\" {\n+    fn zig_cosine_similarity(a: *const f32, b: *const f32, dims: u32) -> f32;\n+    fn zig_jaccard_similarity(\n+        set1: *const u32,\n+        set1_len: u32,\n+        set2: *const u32,\n+        set2_len: u32,\n+    ) -> f32;\n+}\n+\n+// ---------------------------------------------------------------------------\n+// Public API\n+// ---------------------------------------------------------------------------\n+\n+/// Compute the cosine similarity between two f32 vectors.\n+///\n+/// Both vectors must have the same length (`dims`). Returns a value in\n+/// `[-1.0, 1.0]` where 1.0 means identical direction.\n+///\n+/// Returns `Err(InvalidInput)` if either slice is empty or lengths differ.\n+pub fn cosine_similarity(a: &[f32], b: &[f32]) -> Result<f32, KernelError> {\n+    if a.is_empty() || b.is_empty() {\n+        return Err(KernelError::InvalidInput);\n+    }\n+    if a.len() != b.len() {\n+        return Err(KernelError::InvalidInput);\n+    }\n+\n+    // SAFETY: a and b are valid slices with matching lengths.\n+    // zig_cosine_similarity returns -2.0 on null pointer (cannot happen here)\n+    // or on dims==0 (checked above).\n+    let result = unsafe { zig_cosine_similarity(a.as_ptr(), b.as_ptr(), a.len() as u32) };\n+\n+    // The Zig function returns -2.0 as a sentinel for invalid input.\n+    if result == -2.0 {\n+        return Err(KernelError::InvalidInput);\n+    }\n+\n+    Ok(result)\n+}\n+\n+/// Compute the Jaccard similarity between two sorted u32 sets.\n+///\n+/// Both sets must be sorted in ascending order. Returns a value in `[0.0, 1.0]`\n+/// where 1.0 means identical sets.\n+///\n+/// Returns `Ok(0.0)` if both sets are empty (vacuously similar).\n+/// Returns `Ok(0.0)` if one set is empty and the other is not.\n+pub fn jaccard_similarity(set1: &[u32], set2: &[u32]) -> Result<f32, KernelError> {\n+    // Both empty → 0.0 (Zig returns 0.0 for empty sets)\n+    if set1.is_empty() && set2.is_empty() {\n+        return Ok(0.0);\n+    }\n+\n+    // SAFETY: set1 and set2 are valid slices. zig_jaccard_similarity returns\n+    // -1.0 on null pointer (cannot happen with valid slice pointers).\n+    let result = unsafe {\n+        zig_jaccard_similarity(\n+            set1.as_ptr(),\n+            set1.len() as u32,\n+            set2.as_ptr(),\n+            set2.len() as u32,\n+        )\n+    };\n+\n+    // The Zig function returns -1.0 as a sentinel for null pointers.\n+    if result < 0.0 {\n+        return Err(KernelError::InvalidInput);\n+    }\n+\n+    Ok(result)"
  },
  {
    "id": 2812395930,
    "user": "coderabbitai[bot]",
    "path": "zig/src/reference/entities_ref.zig",
    "line": 37,
    "body": "_⚠️ Potential issue_ | _🟡 Minor_\n\n<details>\n<summary>🧩 Analysis chain</summary>\n\n🏁 Script executed:\n\n```shell\n# First, let's see the complete function in context\nhead -60 zig/src/reference/entities_ref.zig | tail -50\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 1447\n\n---\n\n🏁 Script executed:\n\n```shell\n# Search for the complete function definition\nrg -n \"fn extract_entity_hashes\" zig/src/reference/entities_ref.zig -A 30\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 955\n\n---\n\n🏁 Script executed:\n\n```shell\n# Check if there's any null pointer validation in the function\nrg -n \"null\" zig/src/reference/entities_ref.zig -B 2 -A 2\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 273\n\n---\n\n🏁 Script executed:\n\n```shell\n# Find related test files\nfd -t f \"test\" zig/ --name \"*entities*\" -o --name \"*extract*\"\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 313\n\n---\n\n🏁 Script executed:\n\n```shell\n# Find test files related to entities\nfd -t f \"test\" zig/\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 86\n\n---\n\n🏁 Script executed:\n\n```shell\n# Search for tests of extract_entity_hashes\nrg -r \"extract_entity_hashes\" --type zig\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 459\n\n---\n\n🏁 Script executed:\n\n```shell\n# Get complete function to see end\nrg -n \"fn extract_entity_hashes\" --type zig -A 50 | head -60\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 3924\n\n---\n\n🏁 Script executed:\n\n```shell\n# Check if there are any assertions about zero length input in tests\nrg -r \"text_len.*0\\|empty.*input\" --type zig\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 459\n\n---\n\n🏁 Script executed:\n\n```shell\n# Look at the smoke test file to see test cases\ncat zig/test/smoke_test.zig\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 470\n\n---\n\n🏁 Script executed:\n\n```shell\n# Search for any test cases that test zero-length input\nrg \"text_len\" zig/test/smoke_test.zig -B 3 -A 3\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 45\n\n---\n\n🏁 Script executed:\n\n```shell\n# Search for any test that calls extract_entity_hashes\nrg \"extract_entity_hashes\" zig/ -B 3 -A 10\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 13952\n\n---\n\n🏁 Script executed:\n\n```shell\n# Look for test files in the zig directory\nfind zig -name \"*.zig\" -type f | grep -i test\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 86\n\n---\n\n🏁 Script executed:\n\n```shell\n# Check the complete entities_ref.zig file to see full context\ncat zig/src/reference/entities_ref.zig\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 4481\n\n---\n\n🏁 Script executed:\n\n```shell\n# Verify the null pointer dereference situation by checking if text is dereferenced before text_len==0 check\nrg -n \"text\\[0\\.\\.\" zig/src/reference/entities_ref.zig -B 5\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 188\n\n---\n\n🏁 Script executed:\n\n```shell\n# Check what return codes are actually used in the function\nrg -n \"return\" zig/src/reference/entities_ref.zig | grep -A 1 -B 1 \"fn extract_entity_hashes\" | head -20\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 45\n\n---\n\n🏁 Script executed:\n\n```shell\n# Get complete return statements in the function\nsed -n '28,80p' zig/src/reference/entities_ref.zig | grep \"return\"\n```\n\nRepository: sethyanow/markymark\n\nLength of output: 131\n\n---\n\n\n\n</details>\n\n**Align the return-code docs with actual empty-input behavior.**\n\nThe docstring claims `-1 — invalid input (null pointer, zero length)` but the code returns success (0) for zero-length input and has no null-pointer validation. The test `ref_entity_hash_empty` confirms zero length is treated as valid. Update the docs to reflect this:\n\n<details>\n<summary>📝 Suggested doc fix</summary>\n\n```diff\n-///   0  — success\n-///  -1  — invalid input (null pointer, zero length)\n+///   0  — success (including zero length; written=0)\n+///  -1  — invalid input (null pointer)\n```\n</details>\n\n<details>\n<summary>🤖 Prompt for AI Agents</summary>\n\n```\nIn `@zig/src/reference/entities_ref.zig` around lines 24 - 38, Update the doc\ncomment for extract_entity_hashes to match the implementation: remove \"zero\nlength\" from the -1 invalid-input case and explicitly state that text_len == 0\nis treated as success (returns 0 and sets *written = 0); either note that\nnull-pointer checks are not performed or change the -1 description to only refer\nto a null pointer (e.g., \"invalid input (null pointer)\") and list the other\nreturn values (-2 buffer too small) as-is; reference the function and its\nparameters (text, text_len, output_ids, capacity, written) so the docs reflect\nactual behavior verified by ref_entity_hash_empty.\n```\n\n</details>\n\n<!-- fingerprinting:phantom:medusa:eagle -->\n\n<!-- This is an auto-generated comment by CodeRabbit -->\n\n✅ Addressed in commit ed84d6a",
    "created_at": "2026-02-16T13:17:40Z",
    "updated_at": "2026-02-16T13:36:36Z",
    "diff_hunk": "@@ -0,0 +1,146 @@\n+const std = @import(\"std\");\n+\n+/// FNV-1a 64-bit offset basis\n+pub const FNV_OFFSET_BASIS: u64 = 14695981039346656037;\n+/// FNV-1a 64-bit prime\n+pub const FNV_PRIME: u64 = 1099511628211;\n+\n+/// FNV-1a 64-bit hash of a byte slice.\n+pub fn fnv1a_hash(data: []const u8) u64 {\n+    var hash: u64 = FNV_OFFSET_BASIS;\n+    for (data) |byte| {\n+        hash ^= @intCast(byte);\n+        hash *%= FNV_PRIME;\n+    }\n+    return hash;\n+}\n+\n+/// Scalar entity hash extraction.\n+///\n+/// Scans text for word boundaries and produces a FNV-1a hash for each word.\n+/// Words are separated by whitespace or punctuation.\n+/// Output hashes are written as u32 (lower 32 bits of FNV-1a 64-bit).\n+///\n+/// Returns:\n+///   0  — success\n+///  -1  — invalid input (null pointer, zero length)\n+///  -2  — buffer too small (more entities than capacity; writes as many as fit)\n+pub fn extract_entity_hashes(\n+    text: [*]const u8,\n+    text_len: u32,\n+    output_ids: [*]u32,\n+    capacity: u32,\n+    written: *u32,\n+) i32 {\n+    if (text_len == 0) {\n+        written.* = 0;\n+        return 0;\n+    }"
  },
  {
    "id": 2812395936,
    "user": "coderabbitai[bot]",
    "path": "zig/src/reference/similarity_ref.zig",
    "line": 13,
    "body": "_⚠️ Potential issue_ | _🟡 Minor_\n\n**Align docs with actual null-pointer handling.**\n\nDocs mention returning error codes for null pointers, but these functions accept non-optional pointers and do not (and cannot) check for null at this layer. Consider updating the docs to state the non-null precondition or note that null checks are handled at the C ABI boundary.\n\n\n\nAlso applies to: 34-42\n\n<details>\n<summary>🤖 Prompt for AI Agents</summary>\n\n```\nIn `@zig/src/reference/similarity_ref.zig` around lines 4 - 12, The documentation\nfor cosine_similarity (and the similar functions at the same file, e.g., the\nones referenced around lines 34-42) incorrectly claims the function returns\nerror codes for null pointers; update the comments to state that the function\naccepts non-null pointers (precondition: a and b are non-null) and that\nnull-pointer validation is handled at the C ABI boundary or caller side, and\nremove or replace the \"null pointers\" error case from the return description so\nthe docs match the actual signature and runtime behavior.\n```\n\n</details>\n\n<!-- fingerprinting:phantom:medusa:eagle -->\n\n<!-- This is an auto-generated comment by CodeRabbit -->\n\n✅ Addressed in commit ed84d6a",
    "created_at": "2026-02-16T13:17:40Z",
    "updated_at": "2026-02-16T13:36:33Z",
    "diff_hunk": "@@ -0,0 +1,143 @@\n+const std = @import(\"std\");\n+const math = std.math;\n+\n+/// Scalar cosine similarity between two f32 vectors.\n+///\n+/// Returns the cosine of the angle between vectors a and b:\n+///   dot(a,b) / (||a|| * ||b||)\n+///\n+/// Returns -2.0 on error (null pointers, zero length, zero-magnitude vector).\n+pub fn cosine_similarity(a: [*]const f32, b: [*]const f32, dims: u32) f32 {\n+    if (dims == 0) return -2.0;\n+"
  }
]
