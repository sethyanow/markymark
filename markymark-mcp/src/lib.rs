//! markymark-mcp: MCP server implementation using rmcp
//!
//! Provides Model Context Protocol support for markdown tools.

#![warn(missing_docs)]
#![warn(clippy::all)]

use markymark_core::prelude::*;

// MCP will be implemented in TDD Phase 5
// This is a placeholder to make the crate compilable

/// Placeholder function to make the crate compilable
pub fn mcp_placeholder() -> CoreResult<()> {
    Ok(())
}
